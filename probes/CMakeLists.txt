function(ernie_probe name source)
    add_executable(${name} ${source})
    target_link_libraries(${name} PRIVATE ernie-runtime)
endfunction()
ernie_probe(ernie-attention-probe attention_probe.cpp)
target_compile_definitions(ernie-attention-probe PRIVATE ERNIE_NCNN_REVISION="${actual_revision}")
ernie_probe(ernie-block-runner block_runner.cpp)
if(ERNIE_ENABLE_VULKAN)
    target_sources(ernie-block-runner PRIVATE attention_diagnostic.cpp)
    target_include_directories(ernie-block-runner PRIVATE "${ERNIE_NCNN_SOURCE_DIR}/src/layer")
endif()
ernie_probe(ernie-attention-accuracy-probe attention_accuracy_probe.cpp)
ernie_probe(ernie-attention-workspace-probe attention_workspace_probe.cpp)
ernie_probe(ernie-gelu-probe gelu_probe.cpp)
ernie_probe(ernie-latent-runner latent_runner.cpp)
ernie_probe(ernie-block-sequence-runner block_sequence_runner.cpp)
ernie_probe(ernie-head-runner head_runner.cpp)
ernie_probe(ernie-denoise-runner denoise_runner.cpp)
ernie_probe(ernie-text-runner text_runner.cpp)
ernie_probe(ernie-pe-block-runner pe_block_runner.cpp)
ernie_probe(ernie-rmsnorm-probe rmsnorm_probe.cpp)
if(ERNIE_BUILD_TOKENIZER)
    add_executable(ernie-tokenize tokenizer_runner.cpp)
    target_link_libraries(ernie-tokenize PRIVATE ernie-tokenizer)
endif()
if(ERNIE_BUILD_GENERATOR)
    add_executable(ernie-pe-runner pe_runner.cpp)
    target_link_libraries(ernie-pe-runner PRIVATE ernie-pe)
endif()
