# F1 fixed1376/s64 CPU preparation independent review

Preparation a06e40a, actual plan-v2, has one important execution-route finding. No model was executed by this review. Candidate/source identities and shape-only changes pass; the plan is not an executable authorization until the interpreter route and concrete runtime/guard preparation are fixed.

Root independently authenticates all296 source snapshot files,64 source/candidate graph pairs, the saved2,113,536-byte finite FP32 input and frozen head runner. The runner exactly matches the recorded D3 Vulkan build binary. Every graph token outside the enumerated shape fields is unchanged (whitespace normalization is immaterial); all64 complete canonical hashes match the reviewed topology. Counts are25 text,36 DiT and one each input/output/VAE. Target packed86x48 + text64 is4192 total tokens. Text remainss64, VAE becomes172x96; no production registry or s2048 limit is changed.

The actual8x8 VAE template graph and complete198,481,196-byte weight stream match their fixed identities. This is one component only: all64 full source bins have not been independently rehashed in this slice, and the plan truthfully retains weights_content_verified=false. Actual component execution must verify the relevant whole streams first. Six small shape/graph rejection tests pass; these tests do not establish target-resolution model correctness.

**Important: interpreter invocation changes the actual installed module environment.** The plan resolves the supplied .venv/bin/python symlink to /usr/bin/python3.14 and uses the latter in argv. A real, import-free sys.prefix/find_spec check shows the intended invocation selects project .venv/lib64/python3.14/site-packages/torch, while planned argv selects /usr/lib64/python3.14/site-packages/torch. Both interpreters exist and have Torch discoverable; the issue is different actual module provenance, not missing dependencies. Executable SHA equality cannot close this finding. Preserve the absolute virtualenv invocation, record its resolved binary/hash separately, and freeze the actual imported paths/runtime for the subsequent reference process. Retain v2; prepare a new directory.

Two scope corrections accompany this: validate_dit_heads.py accepts --cpu-only --vae-convolution direct, not the plan's descriptive --backend/--precision string; and the frozen head runner internally sets ncnn threads4 for a decoder even under CPU200%/two-CPU affinity, whereas the official PyTorch call selects threads2. Record all three separately; no runner change is required merely to make their numbers equal.

The assigned author is fixing the preparation and will supply a new frozen launcher/runtime/scope contract. GPU and high-memory execution remain unrun. This review has no production acceptance or full shape support conclusion.
