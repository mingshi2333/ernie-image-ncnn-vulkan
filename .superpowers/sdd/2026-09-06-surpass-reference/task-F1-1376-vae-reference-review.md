# F1 1376×768 VAE reference preparation independent review

Reviewed a93a7fc and the finding documented by 771c8ac, against the actual frozen outputs/f1-shape1376-s64-plan-v3. CPU only; no model import, model execution, GPU, pnnx, or full model-package hashing. Plan SHA256: 3718db749c40b7ca60f9aeaad57e58da2a6f07d2b9ce58146093e9808306eb68. All source-identity file hashes were independently checked against the frozen source tree.

## Important I1 — actual model-process runtime identity is not collected (open)

`tools/vae_reference_scope.py:17-41` captures the import-only preparer process (2822 files, 137 mapped files); its own scope explicitly excludes late lazy loads. `:64` and `:89` only verify that same static file dictionary. `:88` delegates the true worker to `check_release.execute`; the frozen `tools/check_release.py:324-356` records cgroup memory, host availability and exit status, but neither worker imports nor mapped libraries. `tools/export_vae.py:68-80` does model construction and two forwards without a worker runtime collector. Thus a newly imported/mapped dependency during those phases has no actual-path/hash/archive evidence and is not rejected. Successful completion is consequently insufficient for the requested complete execution identity.

A small standard-library mock of run() supplied a consistent plan/runtime/source identity and exact expected argv, mocked the cgroup with valid controls, and replaced execute() by a successful worker returning an additional `unobserved_actual_mapped_file` outside the frozen dictionary. The run returned `completed_reference_not_native_validated`. This is a controller-contract counterexample, not a claim that a particular untrusted library was actually loaded by VAE. Source inspection independently establishes that no actual child runtime inventory is requested or checked. The existing two unit tests pass but cover only same-size file changes and venv symlink invocation.

Before execution, add a real worker-side inventory before/after model work (actual imported module and mapped-library paths, hashes, archived source objects), and bind its collector source. Either authenticate newly encountered files against a frozen allowlist or retain them for explicit post-execution review before accepting execution identity. Do not describe the import-only set as a complete forward runtime.

## Verified boundaries

- Venv invocation fix is real: plan preserves `.venv/bin/python` rather than resolving to `/usr/bin/python`; run checks the recorded invocation and `sys.prefix`, while the resolved binary is distinct metadata. The original Important virtual-environment issue is closed.
- Imports of local helpers resolve from the frozen script directory; prepare_block ROOT therefore points at the frozen source tree and its sources.lock.json. All recorded frozen files were checked. Actual captured prefix is the intended worktree venv.
- Input hash is checked by the supervisor and checked again after execution. The exporter checks exact FP32 byte length for [1,32,96,172] and all finite values before model construction. Config and component manifest hashes are supervisor-bound; load_vae streams and verifies both actual safetensors hashes plus prefix/revision before loading their state dictionaries strictly. This is not an all-package weight audit; plan weights_content_verified=false remains honest.
- Exact argv restricts this launch to `--reference-only --fixed-1376x768 --height 96 --width 172 --threads 2`. `_decode` occurs once in export_vae; Decode wrapper occurs once in export_component. Reference-only returns before tracing or importing/calling pnnx. These are two decoder forwards on the same saved unpacked latent, not a generated-image or dynamic-package acceptance run.
- The concrete systemd launcher requests 16 GiB memory.max, swap.max=0, CPUQuota200%, affinity12,14, hidden CUDA, offline mode, thread counts2. The supervisor reads its actual unified cgroup and requires exact memory/swap limits, cpu.max and affinity before spawn. execute checks host availability >=3 GiB before spawn and every50ms, enforces1800s and kills the child's session on failure. A new session retains the cgroup; therefore the memory/swap hard limits cover the worker. This was static verification, not a claim that the model scope has already run.
- Runtime/resource reporting is limited to sampled cgroup memory and host availability; it does not currently claim per-PID RSS or VmSwap samples. No actual model execution or decoder quality result exists in this preparation review.

Verdict: CPU preparation has one open Important execution-identity gap. Preserve v3 unchanged if creating a corrected version. Do not run models as part of this review.
