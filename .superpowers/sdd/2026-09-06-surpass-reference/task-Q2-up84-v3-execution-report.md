# Q2 up84 v3 actual attempt — graphics runtime identity failure

One externally authenticated attempt, session16829; dedicated ernie-q2-up84-v3.scope. Guard stopped at7.801754s on unbound `/usr/lib64/libEGL.so.1.1.0`, child -9, launcher exit1. All experiment processes exited and unique model/GPU slot was immediately released. No retry or runtime/default/guard change.

Native saved no tensors and officialMLP did not launch. No full84 denominator or endpoint invariance exists; no numerical interpretation is valid. Peak sampled recursiveRSS355819520B and wholeGPU1680MiB. Full resource samples and all observed unbound paths, including those after the first rejection in the same sample, are saved in outputs/q2-up84-v3-post-audit/audit.json with actual evidence hashes. Post-stop library hashes are descriptive only.

The dependency collector resolves static ldd closures. Actual graphics initialization can dlopen additional dependencies beyond DT_NEEDED; this attempt shows that its frozen set was insufficient. The exact dlopen caller is not established by maps alone. Repeatedly appending only the first failed library is not evidence of a complete dynamic runtime closure. A useful separate preparation step would observe an explicitly authorized no-model Vulkan initialization/teardown process with the same ncnn library and environment, preserve all actual maps and fixed source/binary identities, then freeze the resulting allowed set for independent review. Such a probe would remain preparation evidence, not a model forward or guarantee of future transient loads; the actual model unknown-map guard must remain unchanged. No such Vulkan probe or additional model run was performed in this turn.
