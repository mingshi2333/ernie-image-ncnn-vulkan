# Q2 up84 v2 actual attempt — loader-cache identity failure

The externally authenticated launcher392e10… and plan9b04ff… were executed once, session29451. Dedicated scope ernie-q2-up84-v2.scope. Guard stopped after0.06315344s on `Unbound mapped runtime /etc/ld.so.cache`, child -9, launcher exit1. All experiment processes exited; GPU immediately released. No automatic retry or guard bypass.

The worker log is empty and native output directory does not exist. No model forward started and officialMLP did not launch. Full84 denominator, endpoint invariance and numerical result are unavailable. This is an execution identity failure, not a quality result.

The mapping collector selects absolute mapped paths containing `.so`, which includes the dynamic loader data file `/etc/ld.so.cache`. The new loader inventory bound resolved library files and saved ldconfig output, but did not bind this actual cache file. Its appearance before Python worker startup therefore correctly triggers the unchanged unknown-mapping rejection. The remedy for any separately authorized new preparation is to bind actual loader-resolution data (cache and relevant configuration/input identities), not exclude cache mappings or weaken the guard. Saved post-stop cache SHA does not retroactively authorize this attempt.

One sample: recursiveRSS31248384B, wholeGPU1646MiB, swap0, cgroup events0; precise mapping PID, scope, host memory and identities are persisted in outputs/q2-up84-v2-post-audit/audit.json. Original attempt files remain unchanged. No additional model/GPU attempt is authorized or executed by this report.
