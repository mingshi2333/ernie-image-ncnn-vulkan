# SDD ledger — implementation in progress

Plan: docs/superpowers/plans/2026-09-06-surpass-reference.md
Worktree: /home/mingshi/Project/AI/ernie-image-ncnn-vulkan/.worktrees/surpass-reference
Branch: codex/surpass-reference. Base b9eb755. User authorized parallel implementation.
F/Q/S/M/D/A gates remain open. No superiority claim or external push/release.

## Completed implementation and measured development evidence

- B1: 101 frozen cases (72 formal, 8 development, 6 performance, 15 img2img); immutable input hashes and independent review fixes in c8835ee. Formal72 and formal15 remain unrun/unrevealed.
- B3: e70fb7a binds six cases, five paired fresh-process repetitions, AB/BA, precision, model/input hashes and failure semantics; 3b3f971 makes unit tests independent of ignored local output. No actual S/M comparison yet.
- B2: peer built with its own pinned ncnn. All28 fixed-revision asset files verified; the actual full CPU scan parsed1129 peer records against1123 official tensors with zero parser gaps:1121 direct matches,1101 unique and20 content ambiguities. Independent fixed-graph review maps859/859 DiT/text/PE/embedding roles and resolves those20 ambiguities; separate VAE evidence covers16 named MHA projections. ea382d4 proves seven of eight remaining records by exact derived/sliced relationships, while88/2048 frequency values differ by1ULP. Peer decoder inverseBN1e-4 differs from official ERNIE1e-5. Artifact artifacts/2026-09-06/reference-weight-identity/README.md distinguishes the original full scan, later graph supplement and remaining semantic gaps. Source-only capacity review finds optimistic21.23GiB persistent BF16 weights/42.45GiB FP32; this is not measured RSS/OOM and cannot establish a speed win. No original peer full image or S/M pair yet.
- Q1: 8e4430f/c64ff31 preserves crossed decoder and trajectory findings. Decoder-only <=7.153e-6 passes; historical full native PE/apple 24/25 decoded .01110548 > .01099488 remains recorded.
- Q2: explicit Vector down-reduction remains opt-in. 0b677d7 seals a REAL complete native greedy PE -> all25 text blocks -> eight Vulkan FP32 steps -> CPU direct VAE at512x384: 25/25, exact315-token PE text/IDs, PNG max1/MAE.0015598. All25 saved tensors match the earlier saved-vector diagnostic bitwise. Native runner ae04f103... predates F1 bridge. No general numerical closure: Chinese Vector22/25+PNGmax13, some downstream errors worse than compensated21/25 baseline. English/1080-token/low-storage historical failures remain.
- Q2 Chinese: fefb93f identifies step6 trajectory amplification, not a proven operator cause. Same-official-input teacher forcing passes; native-time-only swap still passes. 0aeea71 exactly replays all six original native inputs, SHA622738cf...f3fe7, zero differing floats. d9034ad conditional A/B then substitutes only official latent (vs oracle NRMSE1.6919e-5/max.0005716) or only official text (NRMSE.0025988/max.0997976): currentstep divergence primarily follows accumulated latent, without identifying its earlier cause. 04a9f7e confirms identical original prompt bytes/noLF/32IDs across five trajectories and finds official-text step0 prediction NRMSE8.5102e-5; independent Euler arithmetic contributes much less than prediction injection. Full-run22/25 and PNGmax13 remain failed. Next: current a4a runner with exact official head inputs, once through36blocks with layer traces.
- Q3: f73cb5a seals all12 PE development cases, exact text/IDs and independently recomputed1742 logits arrays (each131072 values). Longest actual output526 EOS; near-capacity input2048+output32. Not a2048-output/4096-cache stress proof. Old315-token parity retained.
- F1: reviewed ShapePlan/schema3 CAS connects ModelPackage/ComponentFiles into all native text/DiT/VAE paths without temporary param/weight copies, preserving legacy probes and math. Real shared512 full native PE/text/DiT/VAE passes25/25 and PNG; all25 tensors plus PNG are bitwise equal to the preceding fixed-package run. Artifact artifacts/2026-09-06/shared-native-pipeline/README.md records the original execution and corrected independent audit separately. a77dccc authenticates the entire reviewed oracle and complete25-boundary denominator; independent review ded0f14 closes all Important findings. Only known512x384/s2048 and1024x1024/s64 instances; shared1024 actual generation and arbitrary dynamic shape validation remain open.
- F2: direct CPU FP32 encoder and independently validated512 reconstruction retain encodeBN1e-4/decodeBN1e-5. Reviewed schema3/API/CLI implementation supports trusted optional512 encoder. b07d9ed records ACTUAL production strength0: after full80-object verification encoder/decoder only, six boundaries pass, PNGmax1. Positive strength0.5 ACTUAL native steps4–7 also passes all17 boundaries against the corrected exact-prompt official reference (c20f042): prediction4 NRMSE3.7934e-6, final1.2346e-5, decoded2.2371e-5, PNGmax1/MAE.000803630. The first oracle incorrectly stripped the saved prompt LF; b6bf18b preserves it as invalid_comparison_prompt_bytes with quality=null, not an algorithm failure. The original native bytes were reused without rerun. 791ed83 independently closes oracle source/input/denominator findings. 7faf75a/03fa5ed prepare1024 wrapper and two shape-only changes via shared helpers; actual1024 official/native encoder and production admission remain pending.
- F3: f5d1778 adds bounded PNG/JPEG/BMP/TGA I/O, gray/RGBA/background and Unicode paths; real roundtrip/error tests pass. Public header remains stdlib-only; image input integration joins F2.
- O1: opt-in actual VkDeviceMemory hook/session uses authenticated candidate/peer derived ncnn copies; defaultOFF preserves original source/dependencies. fb65395 connects truthful CLI metrics and device/session lifecycle, independently reviewed by e75eed4. Small actual ON/OFF GPU outputs remain1024-byte bitwise equal,11 allocations/peak2134016bytes/final live0, including importedhost memory. Full64 ON has now exited0 with valid/complete coverage,2375 allocations, simultaneous peak968724992bytes and final live0; identical OFF full run is still running. These are instrumented development/parity measurements, not paired S/M acceptance.

## Current ownership and execution records

- root: B2 graph-role/evidence integration, source documentation and delivery preparation; global GPU/resource serialization and ledger.
- paired_metrics: F2 positive0.5 complete. Owns1024 encoder preparation and bounded CPU official fixture; native comparison follows resource review. No GPU permission.
- frozen_corpus: Q2 conditional sensitivity complete; preparing current a4a exact-head step0/36block trace experiment. CPU preparation only until GPU released by O1.
- reference_adapter: owns O1 full64 ON/OFF generation and output/allocation audit. GPU slot currently allocated through OFF completion.

## Live workloads and safeguards

- B2: full scan81890 completedexit0 in218.7783s, maxRSS52612KiB under3GiB/swap0/CPU2. Graph supplement authenticates prior full scan/official inventory and rereads six param files; it does not repeat the large learned-weight scan. First scope launch failed before process start due missing user-bus environment; corrected to the existing owned bus without altering frozen command/source.
- F2: outputs/f2-positive05-512x384-v1/result-v2.json binds the original native output and separate official-exact-prompt-v2. Both exited0; initial trimmed-prompt evidence retained separately.1024 official CPU reference authorized withCPU2,16GiB cap/swap0 and3GiB host-availability guard, subject to actual free resources.
- O1: outputs/allocation-pipeline64-o1-v1. ON10312 completed; OFF81983 running. Trace-on and potential CPU contention explicitly exclude these clocks from S/M.
- Completed and released: F1 native96587/audit76915; all step6 teacher/replay/A/B diagnostics; F2 native positive suffix and corrected official oracle; O1 builds and actual small GPU probes. Frozen directories remain authoritative.
- GPU queue: O1 full64 OFF -> Q2 exact-head step0 block trace after preparation/review. Large GPU jobs never overlap.
- build-dev executables are current incremental products, not immutable evidence; freeze binaries/sources before each real experiment. Existing outputs/build/ snapshots retain their own identities.

## Required next work

Complete full-model allocation ON/OFF audit and locate initial Chinese block-level error growth. Extend native shapes only with audited graph contracts and independent target-shape fixtures. Close remaining historical full-trajectory failures before default promotion/formal acceptance. Continue reference graph/baseline/capacity work without manufacturing an OOM or speedup from a source lower bound. Build independently usable installed C++/CLI artifacts while experimental quality work proceeds. Complete actual paired speed/memory, platforms/offline delivery and formal quality only after prerequisites pass.

Two independent human blind ratings and actual Windows full-model/offline evidence remain mandatory external evidence. No fabricated review/platform success; they do not prevent useful local work.

All Git mutations use the shared flock .superpowers/sdd/2026-09-06-surpass-reference/git-commit.lock and exact file ownership. Preserve original models, upstream checkouts, old results and all negative evidence.
