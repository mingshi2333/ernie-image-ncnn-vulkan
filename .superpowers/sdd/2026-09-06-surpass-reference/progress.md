# SDD ledger — implementation in progress

Plan: docs/superpowers/plans/2026-09-06-surpass-reference.md
Worktree: /home/mingshi/Project/AI/ernie-image-ncnn-vulkan/.worktrees/surpass-reference
Branch: codex/surpass-reference. Base b9eb755. User authorized parallel implementation.
F/Q/S/M/D/A remain open. Targets are not results. No superiority claim or external push/release.

## Completed implementation and measured development evidence

- Threshold provenance clarification (2026-09-07): user asked whether the gates were assistant-defined; confirmed yes. FP32 pixel max2/MAE.1 and tensor limits are present in first validator commit39678e8, whose comment limited pixels to small-fixture diagnostics, not perceptual quality. S/M targets and sample/blind-review criteria are also project choices. Cross-backend/perceptual calibration remains pending. docs/NUMERICAL-DIAGNOSTICS.md and the plan now separate numerical regression, actual execution and perceptual quality; validator/acceptance JSON and all historical results remain unchanged. No new numerical pass or superiority claim.
- B2 actual original-peer decoder64: pinned8dcd6e4, own ncnn f6f734f4, unchanged assets and binary49422b21. Saved official packed latent SHA872f39cc is identical for CPU/Vulkan FP32. Both native peer commands exit0 and produce64x64 PNG, each against all12288 reference channels: MAE.000732421875/max1, nine channels differ by1. CPU vs Vulkan differs in two channels by1. Both pass historical assistant-selected pixel gates; no NaN claim for peer internal floats, no full denoising or S/M evidence. Fixed plan326 bindings checked before/after; both scopes max/OOM0. See artifacts/2026-09-07/reference-decoder64/README.md.
- B1: 101 frozen cases (72 formal, 8 development, 6 performance, 15 img2img). Original inputs/hashes and independent review fixes in c8835ee. Formal72 and formal15 remain unrun/unrevealed.
- B3: e70fb7a binds six cases, five paired fresh-process repetitions, AB/BA, precision/model/input hashes and failure semantics; 3b3f971 makes tests independent of ignored output. b2053c3 adds native img2img invocation; independent287fa17 found extensionless input, alpha composition and resize identity gaps.8e86a8e/e242de5 fix all three and decode only the immutable copied input; unsupported decoded pixel identities remain formally ineligible.0e8c49f independently checks21/21 tests and actual frozen performance-5 identity. No actual S/M pair.
- B2: peer built with its own pinned ncnn; all28 fixed-revision assets verified. Actual full CPU scan parsed1129 peer records against1123 official tensors:1121 direct matches (1101 unique,20 content ambiguities), zero parser gaps. Independent fixed-graph859/859 DiT/text/PE/embedding roles resolve20 ambiguities; separate16 VAE MHA projections and derived/sliced relationships cover seven of eight remaining records.88/2048 frequency values differ1ULP. Peer decoder inverseBN1e-4 differs from official ERNIE1e-5. Artifact artifacts/2026-09-06/reference-weight-identity distinguishes original full scan from later graph supplement. Source-only capacity bound21.23GiB BF16/42.45GiB FP32 is not measured RSS/OOM or an infinite speed win. No original peer full image/S pair.
- Q1: 8e4430f/c64ff31 preserve decoder/trajectory findings. Decoder-only<=7.153e-6 passes; original native compensated PE/apple24/25 decoded .01110548>.01099488 remains recorded.
- Q2 native PE/apple: explicit Vector text down-reduction stays opt-in. Shared512x384 complete315-token CPU greedy PE, all25 native text layers, eight Vulkan FP32 steps and CPU direct VAE pass25/25 and PNG max1/MAE.0015598. All25 tensors+PNG bitwise equal preceding fixed-package native fixture. No broad quality closure: Chinese Vector22/25+PNGmax13, compensated21/25; English1080-token and low-storage failures retained.
- Q2 Chinese diagnosis: exact six-input replay SHA622738cf reproduces original step6; teacher forcing and native-time-only swap pass. d9034ad A/B shows current divergence primarily follows accumulated latent, not current text alone.04a9f7e authenticates identical78 prompt bytes/noLF/32IDs acrossfive runs and finds step0 prediction injection dominates Euler arithmetic.2d5faee current a4a exact-head36-block run has full-boundary error growth concentrated near blocks13–17; block35 L2=100.9404.64ab75c block15teacher L2=.198777 versus stacked .673117; local/propagated errors partially cancel, so no percentage causal attribution.
- Q2 down-Gemm candidate: 27b8317 three two-input boundary probes75→88→87 each reproduce original out0 bitwise. Final residual is exact full FP32 reconstruction; GELU approximation is not the current priority.4c35148 standalone fixed K12288/N4096 split-K compensation v3 passes both real five-row screens: local L2 improves74.8x/76.5x, max33.1x/65.0x; independent cancellation/range4x4096 exactly matches rounded FP64. Root4d1a2de independently authenticates48 files, full weight transpose and both20480-element FP64 comparisons. v1 reserved-word compile error and v2 invalid upload packing are preserved.02baccf/9cb26be complete block15teacher is an effective negative: all17,039,360 output elements valid, unchanged87, exact five-row screen and exact residual reconstruction; L2 .1987765 -> .370414, max .0065918 -> .0205078. Candidate remains closer to same-input FP64 even on posthoc worst row652, but further step/fullChinese promotion stopped. b083fc6 actual official75/87/88 read-only hooks reproduce complete old out0 bitwise.8f5f878 independently verifies5472 bound entries, full matrix orientation and six-row FP64 decomposition; native/official FP32 rounding residuals correlate at cos.837347, while the closer-FP64 candidate breaks that correlation. Six selected rows are not a full-model causal attribution. No production/default math change or full Chinese repair claim.
- Q2 MLP81: dc0336d maps full native81 to official normalized/modulated MLP input using full param/bin and four named weight tensors. a599abf/1bc3817 add/review fixed outer launcher identity. Actual0c9247d run stops after22.6867s on9,306,112B aggregate process swap: native81 complete/finite and out0 exactly reproduces old teacher, but no official baseline/matched output. This old missing-cgroup resource failure remains. New dedicated10GiB/swap0 preparation2d019b3 is independently authenticated by d51b29d and reuses the valid native outputs. Actual v5 official-only execution929ab28 completes one baseline and one matched81 MLP in37.324s; all four old baseline output hashes match, resource max/swap/OOM remain0. Full-denominator CPU decomposition shows opposing inherited/local terms at87 cos-.7171 and88 cos-.62148, not a single dominant source. Root independent execution/decomposition review recomputes all full denominators, 5905 bound entries, both2743 actual/runtime archives and71 resource samples; all match, exact additive reconstruction residual0 at87/88. No production math change or new full trajectory.
- Q3: f73cb5a12 PE development cases, exact text/IDs, independently recomputed1742 logits arrays each131072 elements. Longest output526 EOS; input2048+output32. Not2048-output/4096-cache stress proof.
- F1: reviewed schema3/ShapePlan CAS connects all native text/DiT/VAE components through ModelPackage/ComponentFiles with no temp param/weight copies. Two known fixed instances512x384/s2048 and1024x1024/s64; no arbitrary shapes. Shared512 full PE-to-image actual25/25+PNG, independent full-oracle authentication a77dccc/ded0f14. Artifact artifacts/2026-09-06/shared-native-pipeline.
- F2 512: actual production strength0 encoder/decoder-only passes six boundaries+PNGmax1. Strength.5 actual steps4–7 passes17/17 against corrected exact-LF official reference: PNGmax1/MAE.000803630. Initial trimmed-prompt oracle retained invalid_comparison_prompt_bytes with quality=null (b6bf18b); original native bytes reused unchanged.791ed83 closes oracle identity/denominator findings.
- F2 1024: actual official/native encoder mean/packed/normalized allpass (NRMSE about7.3e-7), only two reviewed spatial reshapes and unchanged weights. Old wrapper/source-incomplete attempts retained. Independent773b00c/a6d205d and ca24c82 support ec5f245 fixed production registry; other shapes reject. Actual production strength0 passes6/6 and PNGmax1/MAE.000105858; final==encoder-normalized bitwise; observed cgroup peak6,083,784,704B, swap0/OOM0, continuous host guard. Official decoder v2 includes actual installed implementation identity and is bitwise equal v1. e7ce683 independent review closes result scope. Positive.5 bce1be1/edd0ebf completes actual official181.19s/native494.31s: full17 suffix boundaries plus four original encoder/start boundaries, 21/21 total pass; worst prediction6 NRMSE9.155e-6, final4.231e-6, decoded7.704e-6, PNGmax1/MAE.000256856. Official cgrouppeak8,690,458,624B/native6,679,691,264B, swap0/OOM0; these trace runs are not S. c2a9ef6 independently recomputes all21 tensor denominators and PNG, closes source/runner/resource audit with11/11 small tests. Encoder input refers to the earlier actual fixed fixture, then both suffixes ran. Formal15 untouched.
- F2 1024 strength1: actual03da492/cfff405/73f1c1e run all8 absolute steps; exact start-0 equals saved noise. Native808.576s/official252.439s are trace diagnostics, not S. All29 tensors present/finite/authenticated:24 pass, predictions3/4/5/7 and decoded fail fixed max-error gates while all NRMSE pass; PNGmax2/MAE.00514921 passes. f21ad9f independently recomputes complete denominators and actual-execution identities. Status remains quality_gate_failed, no threshold changes or reruns.
- F3: f5d1778 bounded PNG/JPEG/BMP/TGA, gray/RGBA/background and Unicode I/O; actual roundtrip/error tests. Public API remains stdlib-only.
- O1: opt-in actual VkDeviceMemory observer uses authenticated derived ncnn, defaultOFF preserves upstream source. fb65395/e75eed4 reviewed CLI session lifecycle.1a8358c actual full64 ON/OFF outputs are bitwise identical (27 trace files+PNG); ON2375 allocations, simultaneous peak968724992B, all1254 allocator generations inactive/final live0. Trace-on wall368.486/368.159s is diagnostic only; not S/M. New0384d46 adds an explicit private execution collector and honest combined model-load/host-inference intervals; defaultOFF/freshON CLI builds, execution contract and13 fake CLI cases pass. Failure-path0384d46/b08db3e independently reviewed420f6af; stage records flush before trace/callback failures. Frozen64 execution preparation independently reviewed3ec6e4d after samples ownership, source inventory and invalid-data comparison fixes. Actual0fbd387 ON/OFF both exit0; c11ccb3 independently verifies73actualfiles/305sources, all27trace+PNG bitwise and25F32fullfinite. ON2375allocations/peak968724992B/all1254allocatorscleanup; host367.954s has read_prepare320.354s/compute27.459s, knownstage sum366.247s. Coverage is partial, GPUtime/bytes/RSSnull. Parent388.595/382.261s andscope5.85/10.16GB are trace diagnostics, notS/M. New artifact artifacts/2026-09-06/execution-metrics-pipeline64. Artifact artifacts/2026-09-06/allocation-cli-pipeline64.
- D1: installed ernie::pipeline exposes stdlib header, internal static dependencies and exact same-prefix ncnn. CPU/Vulkan v2 relocated Chinese-space prefix, whole-checkout/build hidden, network disabled: actual external configure/link/1 CTest/help/diagnose pass, corrupt package rejected. CPU51/Vulkan75 installed files independently full-hashed.12cd53a cached ncnn_DIR redirection finding fixed and c708d14 actual regression closes it. New GNU16.2.1 CPU preset v5 compiles and passes27/27;267 source identities unchanged.7dab924 independent incremental review also34/34 CLI/bucket. v3 preflight and v4 CPU test failures retained; fixes change tests/fixture location only. GNU16.2.1 Vulkan preset v5 same267-file source config/build succeeds and actual42/42 CTest passes, no skips; LastTest.log confirms NVIDIA kernels. dc44171 independently verifies267 source files/33 binaries/full42 tests, no skips. Source/binaries pre/post unchanged. Build281.29s/test15.76s are resource diagnostics only. Windows/macOS/remoteCI/full-model install chain not claimed. Artifact artifacts/2026-09-06/installed-cpp-sdk.
- D2: downloader67cdafb plus1f1d444 Windows filename fix independently reviewed4d1a2de;22 real localhost/filesystem/schema tests pass.00cf1f4 local CLI draft tools+actualv2 archive10.672MiB,181 complete payload hashes,4 installed files linked to D1,161 project/upstream/Rust notice texts. Root independently rechecks archive and33 combined tests. It remains draft, distributable=false, licenses_complete=false, no public URL; actual linked closure/system notices/platform evidence still pending. Artifact artifacts/2026-09-06/delivery-candidate.

- D2 actual dependency supplement:4dc3a44/ae39ef9 records84 reachable target packages,83 exact crate archives/164 notice texts,572 Rust archive members and513 matching members across19 Rust rlib+1Onig archives, five toolchain notices and eight actual64-bit loader libraries. Rootf6f2f39 independently authenticates173 files, re-parses archives and verifies complete stated associations. Final retained-section/fine-grained toolchain notice closure and redistribution remain incomplete; previous conservative archive metadata is not retroactively changed.
- D3 diagnostics:5887b39 fixes actual v2 unavailable-driver diagnosis failure; v2 stopped before generation and remains archived. New288-source Clang CPU5/5 and Vulkan6/6 affected CTests pass without skips, source/ncnn unchanged.55e58ef/5de7279 independently verify code and new75-file relocated, source-hidden, network-isolated Vulkan installation. New v3 archive d5810af1…/CLI d366c835… is independently bound by0fb6120; only archive/inventory changed from old case, all original inputs and PNG hash gate retained. Artifact artifacts/2026-09-06/offline-linux-diagnostics.
- D3 actual offline: fixed1024 strength.5 passes all nine original command outcomes, complete model verification before/after and full2,891,207-byte PNG identity d426ac2a… in the source-hidden/network-isolated Chinese-space installation. 8131e27 independently authenticates188 preparations/288 source files, actual sandbox arguments and PNG. Generation505.09s has sampled cgrouppeak6,489,657,344B and max0; final verify-model-after reaches10,737,418,240B and cumulative max49634 with no OOM. Original checker never gated max events; retain this resource pressure without claiming S/M or whole-chain peak6.49GB. Status passed_fixed_development_case, distributable/publishedfalse. Artifact artifacts/2026-09-06/offline-linux-generation.

- Q2 up84: root43ce281 independently reviews native81→84→out0 observer and two-forward protocol, 6133 bound entries plus metadata-only correction. Actualv1 d792d7b stops6.515s on an unbound mapped /usr/lib64/amdvlk64.so before tensor output; no official MLP or84result.13samples swap/events0. Freezer incorrectly limited ICD scan to /usr/share *.x86_64.json; /etc AMD was missed. Old attempt stays execution_identity_failed.40d02fa prepares newv2 with loader roots/env/directory identity and ELF class checks,6198bound, originalrunner/math/input/resources unchanged; v2a8daffe prepares/reviews fullinventory butactual87289db stops0.063s onunbound /etc/ld.so.cache beforemodel. v3bb6ad85/f1c8173 adds24cache/config inputs andactualCPUcacheprobe,6251bound; actual22dd472 stops7.802s onthree dynamicallyloadedEGLlibraries beforetensors. Allfailures preserved; noofficialMLP/no84. Nextstep is authorized no-modelVulkaninit discovery to observe actualdlopen dependencyset, originalmodelguard remainsunknownreject.
- F1 fixed1376x768/s64: a06e40a/a93a7fc add isolated candidate and corrected venv invocation. Root771c8ac authenticates296 sources,64shape graphs and fixedVAEbin/input; source arrays/weights unchanged outside approved fields. eff2c34 adds actual worker four-stage runtime capture after frozen6167d50 found import-only identity gap;49ebc10 finds incomplete denominator validation;941a127 addsactualworkerprobe mandatorycoverage/PIDstart identity, independentlyclosed e50cdc6. Actualofficial6833bad completes twoCPUdecoder forwards, full528384input/3170304output finite, fixedinputexact, wrappercomparisonmax/NRMSE0; fouractual2822file/137map boundaries authenticated, no unknown. Scopepeak6.316GB/events0; oldbuslaunchfailure retained. Rootactualreview/nativepreparation pending, no productionregistry expansion; 1376/s2048 remains rejected at6176>6144 sequence.
- D2 final link map: root actual relink of immutable D3 objects is bitwise identical d366c835…;40linkinputs stable, zero newmodel calls. GNUmap selects348archive members+9directobjects with finalSHF_ALLOC contribution. Rust80 links32previously authenticatedproject crate objects+46runtime exact members+2localbridge; oldarchiveSHA6276b286…identical conditions reuse. InitialTLS-VMA doublecount581 corrected by matchingoutputsection; oldanalysis retained. This closes actual-member inventory for thisD3binary only; license/redistribution/platform gates remain open. Artifact artifacts/2026-09-06/linked-binary-dependencies; final independent14e0bc8 verifies allraw/archived identities and357per-source contributions with independentmap/ELFparser, noImportant.

## Ownership and current execution

- root owns all remaining local work. The three agents stopped on account usage limits; they are not running. Their completed results and partial preparations were recovered without external pushes. Model runs remain serial.
- Q2 up84 v5 actually completed one native block and one official MLP in 62.3636s. All previous native81/out0 and official81/87/88 endpoints reproduce bitwise. Full 51,118,080-element same-input up-projection comparison: NRMSE 1.06435388e-6, max 4.00543213e-5. This local rounding difference does not establish the cause of Chinese full-trajectory failure; no default numerical change. Earlier v1-v4 dependency failures and the negative FP64 down-projection candidate remain. Root checks committed c23854e; selected actual records in artifacts/2026-09-07/matched-up-projection.
- F1 fixed1376x768/s64 CPU decoder passed unchanged gates across 3,170,304 outputs (8e28220/c547bfa). The two CPU heads now also pass all nine outputs and all 17,727,488 elements; input-main NRMSE 2.24885847e-7, output NRMSE 1.30240545e-6. All four official/native phases completed with scope events0. Export CLI additions and three small contracts committed c23854e; selected actual records in artifacts/2026-09-07/fixed1376-cpu-components. The 4192-token single block and full36 connected chain also passed; the complete image trajectory remains. Registry has not expanded; no full1376 acceptance yet.
- O2 component timing actual fixed64 regression completed: 27 traces and PNG bitwise identical to saved O1 OFF, all25 FP32 arrays finite, all1320 component rows complete, 2375 allocations and all1254 allocator generations closed/live0. CLI373.0907s; DiT model_load_composite303.8151s includes read/unpack/create/upload/wait. Root corrected only the overly broad nested-interval JSON description; 13 small CLI contracts pass. Selected actual records in artifacts/2026-09-07/component-loading-regression. These are trace-on diagnostics, not S/M or peer superiority.
- Root mapped-loading actual fixed64 completed: all27 traces/PNG bitwise equal to saved O2,25 FP32 arrays finite;64 read-only model mappings observed in the native process. CLI275.7635s versus preceding373.0907s (26.09% lower in this single trace-on observation), DiT composite load206.1195s versus303.8151s; Net destroy increased3.0191s to7.4509s. File-cache state uncontrolled, minor CPU work overlapped, no peer/formal speed conclusion. Vkallocations2375/peak968724992B/all1254allocator generations closed unchanged. Scope reaches10GiB with max466562/OOM0; no RSS or memory advantage claim. Keep experimental defaultOFF; artifacts/2026-09-07/mapped-model-loading.
- F1 fixed4192 block0 and connected36 actual official/Vulkan FP32 complete. Root recomputes all618,135,552 connected outputs in bounded FP64 chunks:36/36 pass, worst/final NRMSE4.37663312e-6 and max.07763671875 pass original gates. Both first-block endpoints reproduce single-block reference/native bitwise; final output equals last trace. Native chain133.6990s/scopepeak9,333,354,496B/wholeGPU4162MiB, official chain415.5584s/scopepeak7,503,912,960B; all four phase max/OOM events0. Source311/runtime2822 files frozen, source/native weights remain original. Artifact artifacts/2026-09-07/fixed1376-block-chain. This is a synthetic hidden/AdaLN connected chain, no heads/Euler/text/VAE complete image and no production registration. Next functional gate is complete fixed1376 native/reference image plus source registry review. All model jobs are now finished.
- Root corrected validate_dit.py entrypoint ignoring main's failed-validation return. Actual entrypoint success/failure return contract checked without running another model; no numerical change.
- F1 fixed1376x768/s64 now completes one actual native prompt-to-PNG case: native 25-layer text (15 tokens, Vector down reduction), all8 Vulkan FP32 steps and CPU direct VAE, PE off/CFG1. Root recomputes all32,098,304 elements; original gates remain FAILED at23/25 (prediction7 and decoded maximum error), PNG MAE0.00504116/max3 versus max2. Exactly4 channel samples across3 pixels reach error3; this does not change acceptance. Native executable returns0, validator/supervisor correctly return1 for quality. Native CLI805.683s, outer889.214s, process RSS6,048,592KiB, scopepeak14,552,866,816B/max0/OOM0, wholeGPU4571MiB. Official outer357.820s, scopepeak17,163,358,208B/max22/OOM0. CPU/memory limits and trace/hash/overlapping package I/O prevent formal performance claims. Initial pre-forward v1 torch metadata/runtime version-field failure is preserved; v2 binds those distinct exact fields separately.
- F1 actual matched-input CPU decoder on the same official final latent passes every3,170,304 output at NRMSE4.3458065e-7/max5.78165e-6; root recomputation agrees,91.778s,scopepeak6,593,490,944B/max0/OOM0. This narrows investigation toward accumulated denoising error; it is a one-sided diagnostic, no individual DiT root cause or full quality closure. Do not repeat already passing synthetic/decoder checks without a new concern.
- Fixed1376 packaging is now implemented in existing tools/package_model.py (--fixed-1376x768), with exact pinned-source/complete-graph checks and no schema3 expansion.20 related tests pass with zero skips. Actual models/turbo1376x768-s64-portable contains136 regular runtime files/23,271,732,244B, no internal symlinks; Python/native full verification passes. Every runtime file/config/source-weight binding matches the actual generation package. Portable manifest cf192a57e7ee8bfc78cea6d8b01698586f78770c324bbbc69ee7b32576e25fb6 differs only in package metadata from the executed development package. Implementation commit e4db644. Actual records and all failures: artifacts/2026-09-07/fixed1376-native-pipeline. All model/packing jobs finished. Next numerical work is the new real trajectory's accumulating DiT error; formal72/15, peer pairing and cross-platform gates remain open.

- Q2 compensated QK actual OFF/ON teacher comparison completes with identical ten inputs/weights and all17,039,360 values finite. OFF exactly reproduces historical175bfa39. ON reduces L2 .198776545→.193962379 but increases max .006591797→.006835938, failing the predeclared requirement that both improve before a full-image run. No full-image promotion; experimental source/CMake flag removed from production, candidate.patch and311-source identity archived. Both23-case operator/cache sets pass with no skips;353 pre/post bound entries remain stable. Both scopes max/OOM0, around0.6GiB sampled cgroup peak, hostavailable≥17.16GB. Preserve the arithmetic negative independently from earlier resource failures. Artifact artifacts/2026-09-07/compensated-qk-experiment. Full quality and surpass-reference goal remain open.

- F1 runtime1376 is now implemented and executed. The pinned schema3 1024/s64 source resolves target1376x768/s64 while preserving the original source config and weights; C++ instantiates the audited graph fields in memory. Retargeting strips the source-sized encoder. Actual all25 native trace tensors and PNG are bitwise identical to the prior fixed1376 run; root recomputes all32,098,304 elements. Native execution returns0, while the historical numerical validator remains23/25 and PNGmax3/MAE0.00504116 with exit1. This completes one functional runtime target, not general dynamic-shape or perceptual-quality acceptance. Current defaults and6144 sequence bound remain unchanged. Actual source311files/459planbindings stable; native775.464s, RSS6,153,812KiB, scopepeak12,328,783,872B/max0/OOM0, hostavailable minimum11,404,804,096B, wholeGPU5246MiB. Trace/hash/CPU-constrained execution is not S/M. Artifact artifacts/2026-09-07/runtime-shape1376.
- F1 shared portable delivery: existing builder creates models/turbo-shared from the two original512/s2048 and1024/s64 sources. All85 regular single-link objects total23,271,662,883B; new manifestdcf56650... and inventory are exactly equal to the executed old CAS store, with separate inodes. Python builder and native full verification both return0. No duplicate inference claimed on the copied package. Rust5/5, shared-store Python21/21 and five affected CTests pass; original CTest exit8 for an unbuilt shape-plan executable is retained and its sole rerun passes after building. Full64 instantiated graph texts equal the independently executed fixed1376 package. All model and packaging jobs have finished; no external push/release. Remaining work is the planned wider shape range, practical quality/performance comparison and delivery coverage; assistant-selected numerical/ratio thresholds remain explicitly distinguished from user requirements.

## Safeguards and evidence rules

- All large CPU/GPU model jobs remain serial. Q2v5, O2 fixed64 and F1 CPU heads completed; the mapped-loading diagnostic and F1 single block completed; F1 connected36 and the complete1376 native/reference image, portable package and matched-input decoder have completed; no model process remains. The full1376 numerical gate is failed, not pending or passed. Root controls the model slot. CPU compiles are bounded to2 cores/4GiB/swap0. These clocks are not formal speed evidence. Following user concern about progress, prioritize existing numerical/functional work and concrete fixes; do not add new diagnostic or report frameworks.
- F2 current workers use observed10GiB/swap0,2CPU,50ms hostavailable>=3GiB continuous monitor,30min timeout. Q2 teacher follows fixed9GiB sampled process-tree RSS/6144MiB wholeGPU/host3GiB guard; these are distinct from exact allocator measurements.
- Frozen sources/binaries/inputs/runner and old results remain immutable. build-dev is a mutable development product, not evidence without a snapshot. Worker preparation failures stay separate from numerical failures and from actual passing process/result evidence.
- Required actual paired S/M, original peer execution/common quality, historic full-trajectory numerical closure, formal72/15, additional declared shapes/devices, PE capacity stress and release/platform checks remain. Two real independent human blind ratings and Windows full-model/offline evidence are external requirements; no fabricated substitutes.
- Continue useful local work while external evidence is absent; do not declare surpassed or mark overall objective complete.

All Git mutations use shared flock .superpowers/sdd/2026-09-06-surpass-reference/git-commit.lock and exact ownership. Preserve original models, upstream checkouts, sources and negative evidence. No external push/release.


## 2026-09-07 runtime range and text buckets

- Root completed the maximum10240-token real-weight block experiment. Device-weight mode crossed the unchanged6144MiB whole-device supervisor guard at6506MiB and was stopped (-9); no OOM, no numerical result. The separate host-weight variant completed, sampled GPU5609MiB/cgroup1,796,833,280B/OOM0. All41,943,040 outputs finite, NRMSE2.7545516415e-6/max.01586914 under unchanged historical gates, independently recomputed by root. Synthetic hidden/AdaLN, one actual block only; no large full-pipeline result or speed ratio.
- Schema3 now uses the bounded16..2048/16-multiple/2MP ShapePlan range, retains verified source indices, and selects available independent32/64/2048 text templates after actual tokenization/PE. Source32 retains64 DiT text slots. Total>6144 uses host Vulkan weights. Legacy1/2 and converters retain their guards. All2496 actual C++ graph instantiations across3 sources/13 geometry points match canonical hashes, without claiming model execution at all those points.
- models/turbo-shared-v2 contains88 regular single-link objects totaling23,271,687,765B, manifest21b6bc8deae17418c22257b52e928ee048a372342a2517bcb954276257c21d1a. Full Python source/CAS validation completed. Actual native64 smoke selects15 valid IDs/32 text bucket/64 DiT slots, completes25 finite tensors (320,768elements),8 Vulkan FP32 steps and CPU VAE, native exit0 and PNG exact to decoded quantization. Supervisor384.779s, cgrouppeak5,832,331,264B, wholeGPU2715MiB/OOM0. Visually the tiny image does not clearly show the requested red apple. This is functional execution, not a quality pass or formal speed round; its new official oracle remains unrun.
- CTests5/5, Rust6/6, Python package-selection6/6/reference8/8/shared-store21/21 passed. The new SDK dependency closure includes ShapePlan; an instrumented-build SDK configure attempt was correctly rejected, OFF restored; new SDK installation/consumer verification remains pending. Preserve earlier full1376 numerical failures and original limits; no threshold recalibration or formal72/15 run. All new model/packaging jobs have finished. Current user asked why work was continuing; root is closing this concrete batch without starting further model experiments. Full canary/quality/performance/platform and overall surpass scope remain open.
- Evidence: artifacts/2026-09-07/runtime-range-and-buckets/README.md. Local changes only; no push/release.

## 2026-09-07 runtime images and installed SDK

- The prior status-only turn was no progress; this batch changes actual implementation and completes new evidence. Root continues alone; all three agents remain stopped. No model jobs remain after this batch.
- Runtime64 gray output is reproduced by complete official modules using the existing saved native initial bytes. Root independently compares all25 tensors/320768elements:25/25 pass, PNGMAE.05029296875/max2; both images are gray textures. Do not infer shape regression, OOM or prompt adherence from the old smoke alone. No repeat native64 run. The original source32 missing-official-hashes rejection is preserved; explicit provenance fallback authenticates the original32 and64 manifests and exact identity of all71non-graph assets. Seven pre-forward selection/rejection checks pass.
- Actual SDK configure exposed missing execution-metrics export. Root adds the archive to the private SDK closure and removes source include-path leakage from its install interface. CPU and Vulkan both install, relocate to Chinese/space paths, hide source/ncnn/models, disable networking, and pass independent C++ consumer/API/CLI checks. Each has8expected command outcomes; corrupted package intentionally exits1. Preserve initial missing archive, system-glslang configure rejection, and executable-only build missing glslang compatibility archives; final Vulkan builds all targets with bundled glslang. No ncnn source edits.
- Runtime512 official CUDA v1 completed step0 then hit the unchanged6144MiB whole-card guard at6155MiB, noOOM. Releasing only unused CUDA allocator cache after each block preserves all8 available first-step/input files byte-for-byte. Separate v2 completes all8steps and CPUVAE,235.771s/cgroup10,460,798,976B/wholeGPU2975MiB/max0/OOM0. Actual image is a red apple on a wooden table.
- Installed native Vulkan runtime512 executes with source hidden and network disabled, same shared-v2 model and saved initial1c89a002...; native text15IDs selects32bucket/64DiT slots,total1088. All25tensors/4,981,760elements pass independent full FP64 comparison, PNGMAE.0003611246744791667/max1 with284of786432 channel samples different. Both PNG quantizations exact. Actual native red apple PNG bf87214d... stored /var/tmp/ernie-runtime512-native-v1/native/native.png. Supervisor466.868s/cgroup13,910,089,728B/hostavailablemin8,083,238,912B/wholeGPU4031MiB/max0/OOM0. Memory.current includes cache and wholeGPU includes desktop; CPU2/trace/hash/cache conditions are not S/M benchmarks.
- Installed binary979f56f377c7fb760e256bd9dcdceb33f3050bc73f115d494ad7ead0e2afe380 is in /var/tmp/ernie-runtime-sdk-v2-vulkan/移动 installation/bin. Actual CPU SDK remains /var/tmp/ernie-runtime-sdk-v1-cpu/移动 installation. Whole fixture registrations03ceedfd... (64) ande68f3d8c... (512) authenticate original source9cc1dc0e..., actual shared source object, allruntime bindings and target geometry. Existing reference tests8/8 and package-selection6/6 pass; actual registered reference checks cover both complete25-boundary fixtures.
- Evidence artifacts/2026-09-07/runtime-images-and-sdk/README.md. Formal72/15, remaining canaries/extremes, Chinese/long text, same-condition peer performance and cross-platform delivery remain open; preserve all prior failures and assistant-selected targets. No push/release, no overall surpass claim. Next useful local work is the remaining practical shape/quality/performance scope, not repeating these passing64/512 tests.


## 2026-09-07 — Runtime rectangles and mapped512 complete

- Installed native384x512 and512x384 both finish source-hidden/network-disabled against the same shared source32 package, with15 real tokens/64 DiT slots/8 VulkanFP32 steps/CPU Vector text and direct VAE. Portrait25/25 and PNGmax1 pass; landscape24/25 preserves prediction7 max0.06594800949 versus0.05711219330, NRMSE0.0006606961 passes, PNGMAE0.002382067/max1 passes. Each comparison independently covers3,605,504 finite elements. Both images were viewed and show a red apple.
- Native portrait426.967s/cgrouppeak5,078,933,504B/wholeGPU3769MiB; native landscape399.537s/cgrouppeak5,540,532,224B/wholeGPU3318MiB. All completed native/reference runs have max/OOM/OOM-kill0. CPU2/trace and uncontrolled cache state exclude formal performance and RSS claims.
- Initial portrait official task stopped at36.774s/wholeGPU6767MiB because of the existing6144MiB guard, not OOM. Same-source rerun with allocation/ownership observation completed at239.793s/wholeGPU3077MiB/owned compute1216MiB. Six saved inputs are byte-identical. First spike cause remains unproven. Landscape official249.021s/wholeGPU3112MiB; no guard event.
- Mapped512 candidate completes all28 trace files/25 tensors/4,981,760 elements/PNG byte-identical to the prior installed512 baseline. Scope466.868->309.793s is one uncontrolled-cache diagnostic, not formal33.6% speedup.61 read-only model mappings observed; peak memory.current16,591,130,624B contains file cache, with16GiB limit/max0/OOM0. In-flight RSS/anonymous/file breakdown is explicitly not a whole-run RSS maximum. Build-dev defaultOFF restored byte-exact979f56f...; candidate89ccaa55....
- Both official references registered by full fixture digests20688b03... and307dbd99...; actual complete25-boundary source/object/shape binding checks pass, retaining native landscape passed=false. Existing reference contract tests8/8 pass. Evidence: artifacts/2026-09-07/runtime-rectangles-and-mapped512/README.md.
- All model jobs in this batch ended. No push/release, no overall surpass claim. Remaining unrun geometry, broad quality, paired performance and platform scope stays open; do not repeat these completed fixtures without a change or unresolved concern.

## 2026-09-07 — Adaptive GPU/RAM weight placement complete

- Following the user's question about real-time GPU-memory handling, implemented `weight_placement.*` and connected it to each Vulkan DiT head/block. Default auto reads the actual compute heap's `heapBudget - heapUsage`, preserves the existing large-shape host preference, and accepts an explicit512MiB reserve plus device/host overrides. This owns no weights or GPU commands; prior completed-command destruction remains.
- 5 affected Vulkan CTests,5 CPU CTests (including installed consumer) and1 Vulkan installed consumer pass. Three actual small Vulkan GEMMs use GPU→RAM→GPU requests under controlled budget changes and return exact `[10,-54]`; actual driver query works. Missing budget/override/overflow/metadata failure cases are covered.
- Complete512 uses live auto queries with intentionally8192MiB reserve to exercise RAM without filling the GPU. All304 requests choose host for budget reasons, no unavailable queries or host→device fallback log. All25 tensors/4981760 finite elements and PNG are byte-exact to the prior verified512 baseline. Binary48db9c4d..., PNGbf87214d...; evidence in artifacts/2026-09-07/adaptive-weight-placement.
- Scope526.741s, wholeGPU2363MiB,cgrouppeak4618027008B includes file cache,minimum host available14738100224B,max/OOM0. This is a trace-on diagnostic, not a paired speed or memory result; no speed gain claimed. GPU activations still require memory. Full residency spill, OOM recovery, cross-step prepared-weight reuse and RAM budgets remain open; O2 and the overall goal are not complete. All jobs in this batch finished.


## 2026-09-07 — Bounded prepared RAM weights implemented and exercised

- Root added request-local `WeightSession` with move-only leases, fixed subset admission, measured host-only Vulkan weight-buffer inspection, and Linux host/cgroup pressure eviction. Optional `--dit-cache-mib` defaults0; `--ram-reserve-mib` defaults3072. GPU FP32/auto-or-host only, no worker/prefetch. Active leases outlive cancellation; all cached Nets release before VAE. Computation inputs/outputs and exact DiT semantics remain unchanged; reusable attention submission counters now contribute deltas.
- 7 Vulkan affected CTests +6 CPU affected/installed tests +1 Vulkan installed consumer all pass. Fake-loader/lifetime tests cover scan reuse beyond capacity, pressure, identity, failure, move, cancel and early owner destruction. Actual3-round Vulkan GEMM retains2 Nets, hits4/loads5, returns exact[10,-54]. Initial service-cwd and symbol-namespace build failures were fixed before model execution. Original pinned ncnn remains clean.
- Actual512 cached run binds387 sources/binary/inputs/baseline records, source hidden/network disabled/model read-only. Same native15-token Vector text/source32/64DiTslots/initial/8FP32 steps/CPU direct VAE. All25 tensors/4981760 finite values and PNG are byte-identical to the prior no-cache/native baseline. With explicit6GiB cache/3GiB reserve,6 blocks yield42hits/246loads/0evictions/0missingqueries, cachechargepeak5687678976B.262 actual RAM requests including16heads; no device fallback logged. This continues the prior deliberately8192MiB GPU reserve to exercise RAM; it is not the512MiB default or actual GPU exhaustion.
- Scope469.112600854s versus earlier no-cache RAM526.741229893s is an uncontrolled-cache single diagnostic, not a formal paired speedup. WholeGPUpeak2376MiB includes desktop, cgrouppeak12277387264B includes file cache and is not RSS, hostavailableminimum9870577664B; max/OOM/OOM-kill0. Binary93817d84..., PNGbf87214d..., plan684b627f.... Evidence artifacts/2026-09-07/bounded-weight-session.
- All model and supervisor processes finished. No push/release, no overall superiority claim. Partial O2 is now functional; one-block prefetch, all8 development cases, repeated same-binary timing, complete host/device measurement, broad numerical/quality/shape/platform acceptance remain open. Default cache staysOFF until broader evidence.

## 2026-09-07 — Runtime loading policy and actual cache-pressure negative result

- Added public/CLI default|stdio|mapped loading policy, resolved in a small internal header and applied by the image pipeline; default retains the existing CMake choice. Reported mapping is explicitly a request because ncnn can fall back to file reads. PE is unchanged. Generation still defaults to automatic GPU/RAM placement, mapped/cache remainOFF.
- Ten effective Vulkan checks and six CPU checks pass, including installed C++ consumers. CPU small real GEMM verifies FP32/BF16 file modes, both policy defaults, repeated extraction and Linux mapping lifetime. Vulkan cache tests cover stdio/mapped and both file formats with exact outputs and4hits/5loads. Initial new-target Makefile issue and the new test's incorrect Net::clear lifetime assumption are retained; source inspection established Net destruction as the actual mapping-release boundary, matching production cache eviction. Only the failed lifetime test was repeated after its correction.
- Frozen390bindings full512 adds --model-loading mapped to the previous6GiB RAM cache/3GiB reserve and deliberately8192MiB GPU reserve fixture. Native15-token Vector text, source32/64DiTslots, savedinitial,8 VulkanFP32steps andCPUdirectVAE; source hidden/network disabled/model read-only. All25 tensors/4981760 finite elements andPNG exactly match the original verified512. Binary8383542c..., plan9880c5f4..., PNGbf87214d....
- Actual0.5s samples observe58read-only model mappings. Cgroup reaches17179869184B with max45089, OOM/OOM-kill0; wholeGPUpeak2372MiB includesdesktop, hostminimum10789851136B. Chargedcachepeak5687678976B/6Nets;12admissions/12evictions/**0hits**/288loads/unavailable0. Fullscope386.095638291s is a traced diagnostic, not repeated paired timing or RSS. The original minimum-hit checker returns1 and remains unchanged; the additional review reports numerical agreement and bounded charges but all_checks_pass=false.
- Evidence archived in artifacts/2026-09-07/runtime-model-loading,29smallrecords226767B plusREADME/inventory. This verifies continued generation with actual cache-pressure eviction, but does not prove a useful cache+mapping combination. Defaults remainOFF. O2/S/M/fullquality/shapes/platforms remainopen. All jobs finished, no push/release.

## 2026-09-07 — Host headroom and cached-hit reservation

- Root splits Linux host/cgroup availability into src/host_memory.*, with strict uint64 parsing, current membership re-read, all finite ancestors, conservative clean inactive-file credit, and saturating arithmetic. Dirty/writeback/shmem exclusions remain; anonymous/pinned weights, active_file, slab and swap get no extra credit. Missing optional statistics give zero credit; missing required data disables admission. A cached hit needs the reserve without reserving a duplicate weight payload; actual reserve pressure still evicts idle entries.
- Initial10Vulkan+6CPU checks pass; after the final file-list/type-count clarification, both builds and targeted7Vulkan+3CPU checks pass without skips. Preserve both rounds; do not count repeats as distinct coverage. Tests include memory pressure, ancestor limits, migration, invalid counters, overflow, duplicate-reserve regression, real mapped/stdio Vulkan weight reuse and installed C++ consumers. Pinned ncnn6a1bf000 remains clean.
- Frozen394bindings full512 preserves the preceding mapped+6GiB cache/3GiB RAM reserve/deliberate8192MiB GPU reserve arguments and16GiB cgroup/swap0/wholeGPU6144MiB guards. Source hidden/network disabled/model read-only;15-token native Vector text,source32/64DiTslots,savedinitial,8VulkanFP32steps,CPUdirectVAE. Original comparison exits0: all25 tensors/4981760 finite elements andPNG byte-identical to baseline,13hits/275loads/54admissions/54pressure evictions,chargepeak5687678976B/6Nets/unavailable0.291RAM requests including16heads,noGPU fallback logged,59observed readonly mappings. This is controlled RAM selection, not actual GPU exhaustion or recovery.
- Scope378.970461378s is traced/unpaired, not formal performance. WholeGPU2339MiB,cgrouppeak17179869184B,max133919,OOM/OOMkill0; hostavailableminimum12560400384B.0.5s native samples observe VmHWM9800302592B,VmRSSpeak9760870400B,RssAnonpeak6569431040B,RssFilepeak3263877120B,VmSwap0. Observed HWM may miss a later exit peak; different category peaks cannot be added. Cgroup file-cache-inclusive usage is not process RSS.
- Same minimum-hit requirement now passes, but reuse is limited and pressure churn remains; previous0hit/12eviction failure is preserved. Do not call this stable retention, assign causal shares to the two fixes, or infer a speedup from earlier386.096s. Next bounded-cache work should address repeated admission/eviction and use controlled repeated same-binary timing before selecting defaults. No activation spilling, failed-command recovery or cross-platform host readers added.
- Evidence artifacts/2026-09-07/cache-headroom:33smallrecords323173B plusREADME/inventory. Binary115e4258...,plan59ccefdc...,PNGbf87214d.... Final archive revalidates all394 bindings and finished nativePID2841441. All jobs finished, defaults remainOFF, no push/release. O2/S/M/fullquality/shapes/platforms and the full surpass goal remain open.

## 2026-09-07 — File LRU state no longer drives cache eviction

- Prior goal turn made verified progress:007f0f4 committed the host reader and complete13hit/54eviction regression. Current turn confirms clean worktree and finished prior jobs, then replays722 prior leaf-cgroup samples: inactive-only estimates fall below3GiB on270samples; this is retrospective diagnostic data, not exact acquisition/ancestor evidence.
- Root authenticates Linux mm/show_mem.c at df2908090cda368b01ff43709f51890076c56157. Its available-memory calculation includes active+inactive file LRUs and retains min(half,low watermark). Project host_memory now credits only half of the bounded clean file LRU total; no new anon/pinned/slab/swap credit, all host/ancestor caps remain. Tests cover invariant movement between lists, odd rounding, overflow, dirty active pages, missing/duplicate counters and existing pressure/migration contracts. Final7Vulkan+3CPU/installed-consumer checks pass, no failures/skips.
- Frozen396bindings fullmapped512 uses identicalnativeargs,6GiBcache/3GiBreserve/deliberate8192MiB GPU reserve,16GiBcgroup/swap0/wholeGPU6144MiB/host3GiBguards. Source hidden/network disabled/modelreadonly. Same15-token Vector native text/source32/64DiTslots/initial/8VulkanFP32steps/CPUdirectVAE. All25 tensors/4981760 finite elements andPNG byte-exact.6admissions,42hits,246loads,0pressure evictions,6Nets/cachecharge5687678976B,queriesunavailable0.262RAM requests including16heads,noGPUfallback logged,64readonlymodelmaps. Frozen goals morehits/fewerevictions than13/54 pass; this does not close O2 or formalperformance.
- Scope384.363937947s versus preceding378.970461378s gives no speedup evidence and is traced/unpaired. Cgroupmax354168 rises from133919; OOM/OOMkill0. Actual cgrouppeak17180102656B is233472B over configured17179869184B; retain this transient observation allowed by kernel limit semantics. WholeGPU2349MiB,hostminimum12778840064B,observednativeVmHWM9801760768B/VmRSSpeak9793703936B,VmSwap0. Category peaks occur at different times; cgroupincludesfilecache and is notRSS. Do not call stable retention in this singlecase a memory or speed win.
- Evidence artifacts/2026-09-07/cache-file-lru:29records279156B plusREADME/inventory, binaryce64aa5b...,plan005f3d8a...,PNGbf87214d.... Archive verifies396bindings,cleanpinnedncnn,andfinishednativePID2864196. No jobs remain. Alloldnegative/partialresults preserved; mapped/cache remainOFF; no push/release.
- Next concrete performance work: tools/benchmark_pipeline.py still calls schema1/2-only verify_package, assumes manifest.config, lacks runtimewidth/height and loading/cache/weight-placement/thread controls, and old VAE timing regex misses current CLI wording. Existing shared verification/selection helpers in package_dynamic_model.py and pipeline_package.py can be reused. tests/test_port_metrics.py exercises the old mocked fixed-package path. Fix this actual tool gap, bind effective settings and real native selection, then use same-binary traceOFF repeated comparisons; do not weaken frozen formal identity requirements. Fullgoal,O2/S/M,shape/quality/platform acceptance remainopen.

## 2026-09-07 — Trace-OFF benchmark consumes verified native completion records

- Previous goal turn only answered the user's dynamic GPU/RAM question after live inspection; classify as no implementation progress. No blocker or live old job remained. Root resumed the concrete benchmark gap instead of repeating the passed cache regression unchanged.
- Added cli/generation_report.* (standard C++ public types, JSON escaping and exclusive new-file writes), actual post-tokenization source and GPU fields in GenerationResult, and --report-json. New benchmark_request helper authenticates fixed/shared packages, respects fixed geometry, selects shared source using actual returned token IDs, and verifies native settings/shape/progress/counters. Wrapper now passes dimensions/threads/device/text reduction/loading/placement/cache/reserves, snapshots and verifies finite correctly sized FP32 noise, and rechecks executable/input/manifest identities. Existing formal port_metrics protocol and image-decode identity policy preserved. NVIDIA sampling index is independent and explicitly unverified against Vulkan index; optional sampler disable supports non-NVIDIA runs. Trace and allocation instrumentation exclude speed eligibility; logs do not establish source, settings or timing.
- Initial12CPU/Vulkan/installed-consumer CTests and36Python tests pass, no failure/skip. Frozen669bindings actual shared512 with original project hidden/network off/model+tool snapshot readonly; same saved noise,15token native Vector text/source32/64DiTslots,8VulkanFP32steps/CPUdirectVAE,2threads,GPU0,mapped,deliberate8192MiBGPUreserve,6GiBcache/3GiBreserve; trace/instrumentationOFF. PNG byte-exact baselinebf87214d...; no new intermediate tensors and no new official quality count. Actual262RAM requests,42hits/246loads/6blocks/0pressure evictions,charged5687678976B,queriesunavailable0.
- Native external386.853920829s, internal386.326069704s, whole supervisor404.478277155s (includes Python preverification). Native GNU time RSS9560916KiB; wholeGPU2336MiB; file-cache-inclusive cgrouppeak17180102656B, max347682, OOM/OOMkill0; configured16GiB/swap0/host3GiB/wholeGPU6144MiB unchanged. Native VmSwap samples0 but global SwapFree changed; do not assert whole-system swap0 or causal attribution. File cache/background conditions uncontrolled; raw prerequisites eligibility is not paired speed/quality acceptance.
- Full run20ab4f77... completed and comparator verified frozen sources before a reporting correction: text progress is elapsed(text_start), not cumulative. Corrected only that descriptive string and restored legacy text_seconds result key; added a compatibility assertion and conventional optional test-runner path. Finalff2f934f... rebuilt CPU/Vulkan, affected3+3CTest/9Python checks pass, then both JSON CTests pass after harness change. Full model not rerun for metadata-only changes; raw files retained and exact4-file changes disclosed in scope-correction/post-run-source.diff/final-review. Current validator accepts the actual native run. Do not claim full-model execution of the final relinked binary.
- Archive artifacts/2026-09-07/benchmark-runtime contains41smallrecords402248B plusREADME/inventory; plan10b17b42..., nativePID2900979 finished, ncnn6a1bf000clean, no jobs remain. No push/release. Next action is frozen same-final-binary repeated mapped/stdio and cacheOFF/ON development comparison; broad quality/canaries/platform delivery, O1/O2/S/M and full superiority remain open.

## 2026-09-07 — Frozen four-configuration memory/loading grid started

- Root prepared a new same-final-binary repeated development grid, rather than repeating a prior single passing case. Actual binary ff2f934f... from clean b6b80d3, source snapshots, fixed shared512 inputs/precision/threads/Vector text/CPU direct VAE. Four configurations stdio/mapped × cache0/6144MiB; explicit host weights in all runs, unchanged RAM reserve3072MiB, trace/instrumentationOFF. One warmup plus three measured per configuration, fixed seeded order/rotations, sixteen fresh processes. This is not auto GPU pressure/OOM recovery and does not replace the formal peer S/M corpus.
- Protocol frozen before execution: grid SHA04d0e4e7454436853e4031e1eee55d367cfb2a2edd33038c71eb7319081d8a1b,374common bindings plus16subplans. Readonly source snapshot allows separate repository work; never edit the frozen files. Six synthetic orchestration checks and isolated preflight pass; failures stop without dropping cases or retry, no aggregate until all sixteen pass. Resource controls remain16GiB/swap0/host3GiB/wholeGPU6144MiB/CPU2/1800s. Artifacts/2026-09-07/memory-grid-protocol records preparation only, not full results.
- Batch launched and live master2938116/supervisor2938152 were verified on boot493a7379-3038-4d08-b67f-8e471d0d807f; unified exec session63899. Current trial at this note:00-warmup-mapped-off, zero finished. Authoritative mutable status:/var/tmp/ernie-memory-grid512-v1/progress.json and per-trial native/process.json; master.log/results.json preserve all records. Do not assume these PIDs remain live or restart on a stale status: inspect the recorded processes and session first. Estimated batch about2h; no result or speedup claimed yet.
- User's memory question was answered from current code: default auto rechecks GPU budget before each component and requests RAM weights; optional bounded FP32 RAM cache evicts idle blocks under Linux headroom pressure. Activations/workspace remain device-side; no failed-command recovery. Prior actual512 auto-RAM and cache regressions already pass, do not rerun them unchanged. Defaults remain mapped/cacheOFF; full quality/shapes/platforms/O1/O2/S/M/surpass scope remains open. No push/release.

## 2026-09-07 — Compact ordinary-read buffer candidate implemented

- Previous goal turn made concrete progress by freezing, starting and committing the repeated grid asd3e14a8. Root verified the live master/supervisor and continued independent CPU-side work while the frozen model jobs run serially. At this note all4warmups completed with byte-exact baseline PNGs, zeroOOM/OOMkill; first measured trial04-measured-mapped-cache is active. Warmup times are not aggregate performance results. Inspect actual progress before acting; never restart the batch based on this historical note.
- Pinned ncnn ModelBin FP16/BF16 ordinary-read vectors use byte counts as unsigned-short element counts. Added defaultOFF authenticated build-directory correction, changing exactly2resize expressions; originalSHAeefc5384...,derivedSHA90adcd77...,upstream6a1bf000 remains clean. Reader allocation contract observes actual vector requests and independently checks FP32 bit patterns, padding, mapped-reference reads and missing payload/tag rejection. CPU/Vulkan Release builds each pass the3affected CPU tests OFF andON:12executions, nofailure/skip, noGPUwork. For8,388,612B payload the array is16,777,224B OFF and8,388,612B ON; not whole-process RAM or speed evidence.
- Candidate CPU/Vulkan binaries/probes saved outsideGit; VulkanCLI b15ccce8.... Both normal builds restoredOFF, each ncnnarchive byte-exact baseline andVulkanCLI ff2f934f... exact. Added CPU-ON Linux CI matrix entry is locally parsed only; remoteCI notrun. Source/hash rejection, all12test logs, candidate/config identities, default restoration and source inventory preserved at artifacts/2026-09-07/compact-model-reader. LocaldefaultOFF, no push/release. Next candidate task is fullmodel regression after the activegrid finishes, then separately bound repeated measurement if justified; currentgrid cannot measure thisnewpatch. Fullsupersedinggoal, broadquality, shapes, platforms andO1/O2/S/M remainopen.

## 2026-09-07 — Windows CPU portability and UTF-8 repair

- While the frozen memory grid continued serially, root completed independent CPU-side P4 development using MinGW GCC 16.1.1, Rust 1.98.0 Windows GNU target and Wine 11.0. Explicit target selection, platform static-library naming and Windows system links fix cross-compilation; the POSIX resource/PID interfaces have Windows branches. Exclusive report creation now uses `_wopen` flags after the actual CRT ignored `fopen`'s x suffix and overwrote old output. Windows CLI has a separate UTF-16 entry, UTF-8 public strings and native-path file boundaries, including ncnn wide loads. Model math, weights, precision and memory defaults stay unchanged.
- Actual final Windows C++ 28/28 and CLI/report 35/35 pass, no skip/failure; final Linux CPU CTest 35/35 passes. All 334 source files reconstruct exactly from base8158dc1 plus archived source.patch. Final Windows CLI 5c1706e89d8137d408e2b2fc459e6eb34ba778b5eb27bc578e4f06e6a12e4209 matches the actually relocated SDK v4. V4 source/build hidden, network disabled, moved Unicode prefix, foreign-ncnn sentinel and external C++ consumer: all13 expected outcomes, including three expected rejection exit1 values. Its owned Wine prefix exits cleanly. CPU jobs use separate cores,4GiB cgroups/swap0; no new full-model case.
- Original initial build failure,26/27 report-writer failure,59/62 Python run with3POSIX symlink failures, SDKv1 readonly temporary-directory failure, SDKv2 Unicode path error123, and SDKv3 cleanup timeout remain archived. Windows-created file/directory/PE links are identified and rejected in3additional scenarios, without changing/skipping old cases. Early metadata helper source was not frozen; its later extended source is disclosed separately rather than assigned a fabricated old hash. Wine performance and links do not certify native Windows/MSVC/Vulkan/full-model acceptance.
- Evidence and build instructions are in artifacts/2026-09-07/windows-cpu-portability and docs/BUILDING-WINDOWS.md. Linux Vulkan build-dev was also rebuilt after these path changes, so it is no longer the older ff2f934f binary. The active16-run memory grid remains bound to its original ff2f934f readonly snapshot/source and was not restarted; historical status at this entry is15complete and final15-measured-stdio-off active. Inspect live progress before acting. The compact-reader candidate likewise remains a distinct8158dc1 snapshot. Full quality/canaries/platforms/O1/O2/S/M/P4 and superiority stay open; no push/release.

## 2026-09-07 — Repeated memory grid complete; compact candidate running

- Windows portability and evidence saved locally as7d61d18; compact-reader preflight protocol as74fac55. All16 frozen grid trials finished without restart/discard: PNGbf87214d... exact, native/cgroup OOM/OOM-kill0, observed native VmSwap0. Master exited, unified63899 completed0. Re-running the frozen analyzer validates374common bindings and16subplans and reproduces resultsSHAf26721c4f852acb4697720109e88394d4821338b909428d63f76700ea0a354f7 exactly. No model rerun for this recheck.
- Per-setting three measured wall medians in seconds: stdio-off500.252, stdio-cache465.762, mapped-off395.931, mapped-cache393.330. Native RSS medians inGiB:1.824/6.276/1.947/9.102. Paired median wall ratios imply20.85% observed time reduction for mapped-off vs stdio-off,6.10% for cache in stdio,0.85% for cache in mapped. Every cache trial42hits/246loads/6blocks/5687678976B charge/no pressure eviction. Mapped has cgroup max events, max observed17180057600B (188416B above configured16GiB); stdio has max0. All16 wholeGPU peaks2284..2391MiB include desktop; RSS/cgroup/cache/GPU scopes remain distinct.
- Results and every trial's small command/report/process/exit/resource records archived at artifacts/2026-09-07/memory-grid512. Large raw samples/PNG/latents/binaries stay locally with hashes. Page-cache state and concurrent independent CPU work uncontrolled, only one512fixture, noformalpeer S/M/fullquality claim and no default change. Cache remainsOFF because mapped's sub1% paired gain does not justify severalGiB extra native RSS; mapped itself remains opt-in pending wider evidence and file-page-pressure assessment. Auto GPU/RAM policy is alreadyON, but all these runs explicitly requestRAM and do not test exhaustion/recovery.
- After terminal checks, the preregistered compact-reader candidate started from /var/tmp/ernie-compact-reader512-v1 in unified session72431. Plan45d43302..., candidateb15ccce8..., frozen8158dc1 source,stdio/host/cache0,512/8FP32steps/VectorCPUtext/directVAE/traceON. Output directory was absent before launch. Supervisor requires old grid complete and master exited before creating output. This is a changed-reader regression, not a repeat of unchanged inference or a timing comparison. Actual result pending at this entry; inspect live process state, keep failures and never overlap model jobs. No push/release; all remaining formal quality/shapes/platform/superiority requirements remain open.

## 2026-09-07 — Compact reader full-model regression complete

- Unified72431 returned0; nativePID3102831 ended and its cgroup is removed. Frozen comparison verifies all374bindings and all actual command/report settings. All25 finite tensors/4,981,760 values and PNGbf87214d... are byte-exact against the previously verified512 RAM baseline; every max-absolute difference is0. All8FP32steps, native15-token Vector text/source32/64slots,CPUdirectVAE,PEoff;304RAM requests/no cache/no logged fallback. Max/OOM/OOMkill0.
- Whole supervisor443.708785047s includes preparation and trace; cgrouppeak2817376256B includes file cache, sampled nativeVmHWM2085281792B, wholeGPU2308MiB including desktop, hostminimum16994439168B. These are distinct scopes and cannot be summed or compared as formal improvements against the traceOFF grid. DefaultOFF compact reader staysOFF. This is full-model numerical regression of the changed buffer allocator only, not a new official quality sample, actual exhaustion/recovery test or Windows path test.
- Archived artifacts/2026-09-07/compact-reader-regression with original command/report/process outputs, comparator and retained raw-file hashes. Earlier protocol/preflight and every old failure remain. Sourcebaseline8158dc1/candidateb15ccce8/plan45d43302 stay distinct from Windows7d61d18/currentcode. No model jobs in this batch remain, no repeated passing runs, no push/release. Native Windows/MSVC/GPU/macOS, broader shapes/quality and formal peer S/M/overall superiority remain open.

## 2026-09-07 — Full shared 768 and 1024 canaries started

- Root answered the dynamic-memory question against current source: default per-component auto GPU/RAM weight requests exist; optional bounded Linux RAM cache evicts idle entries; activations/workspace remain device-side and failed Vulkan commands are not recovered. The existing exact controlled512 proof does not certify true exhaustion.
- With previous model jobs ended, froze current990e8ef source334files/Linux92559ea4 binary, sharedsource32 and two saved initial arrays for runtime768x768 and1024x1024. Fixed host weights/stdio/cache0,8VulkanFP32steps/CPUVector text/directVAE/PEoff/traceON, each430bindings. Same historical limits/gates; not speed, auto-pressure or OOM-recovery measurement. Both network-disabled, native original-project-hidden; all4actual preflight outcomes pass. Protocol artifacts/2026-09-07/runtime-squares-protocol records original plans and launch. Unified62247 batch is running first768native at this entry; inspect /var/tmp/ernie-runtime-squares-v1/results.json and actualprocesses before continuing, never duplicate. Full results pending. Every previous failure/default/full-goal limitation stays; no push/release.

## 2026-09-07 — Windows memory reader with unsupported-limit fallback

- Root added Windows GlobalMemoryStatusEx physical/commit/address-space minimum to the existing host_memory module, with no pagefile-as-RAM credit. The first direct API candidate passed its4CPU/Wine checks but reported18668998656B inside a configured4GiB Linux scope; that value is unsuitable for cache admission under Wine. Preserved original source/tests and the initial empty-diagnostic wineserver-p60 exit2 before Windows tests; the later actual2Windows tests passed after removing that unneeded helper.
- Final reader disables optional-cache admission under Wine or any Windows Job, rechecks Job membership around the memory query, and retains streaming. NULL QueryInformationJobObject exposes only the immediate Job, so all enclosing bounds remain unproven; no invented ancestor headroom. Actual final Wine API17542742016B versus productionunavailable; actualCreate/AssignProcessToJobObject plus independently verified membership also returnsunavailable from the same reader. Native Windows outsideJobs uses OS estimates, but its cache/GPU/fullmodel path still needs native validation; macOSreader staysopen.
- WindowsMinGW andLinuxCPU builds pass; finalhost_memory_cpu/weight_session_cpu2tests on eachplatform allpass, WindowsCLIhelp0 and ownedWineprefixstop/wait0. Four source files reconstruct exactly from0836b78 plusv2source.patch. FinalWindowsCLI46df787c...,LinuxCPUCLI30cae0f3...,probes snapshotted outsideGit. Evidence artifacts/2026-09-07/windows-memory-headroom contains bothcandidates, fullbuild/testlogs and hashes. No math/precision/cachedefault changes, no LinuxGPUmodelrerun, no push/release. Frozen768/1024 jobs retain their separate990e8ef/92559ea4 snapshots.

## 2026-09-07 — Full shared 768 and 1024 canaries complete

- Both frozen native/official pairs complete all 8 steps and VAE, command exits 0, cgroup max/OOM/OOM-kill 0. 768 strict tensor gates remain 18/25, PNG MAE 0.0277167426/max2; 1024 is 24/25, PNG MAE 0.0007610321/max1. The latter's only failed boundary is prediction-7 max0.0830631852 versus limit0.0586947634. The 768 case first exceeds a maximum-error gate at prediction-6, with prediction-7 also exceeding NRMSE0.003. These are numerical findings, not memory failures; causes and wider perceptual calibration remain open.
- Root independently reread each430 bindings and actual execution/request/IDs/initial provenance, recomputing every tensor and PNG from 13,829,120 and31,720,448 finite elements. Native images visibly contain the requested red apple on a wooden table; this is not formal blind review. Native scope times642.808983/849.934447s include preparation/trace, cgroup peaks13847592960/11215736832B include file cache, wholeGPU peaks2821/3338MiB include desktop. Explicit RAM/stdio/cache0 and304RAM requests each do not establish auto-pressure handling, OOM recovery or formal peer speed/memory advantage.
- Original62247/768 batch stopped on numerical comparison exit1 after native0/official0. Once its master ended, separate continuation78597 ran only the originally frozen1024 commands, also native0/official0/compare1; plans, inputs, binaries, resource guards and gates unchanged. Both unified handles are terminal; current process inspection finds no surviving canary/continuation jobs. All original results and failures remain in artifacts/2026-09-07/runtime-squares. No unchanged model reruns.
- Official whole fixtures731ebbab (768) anddbfd637a (1024) now registered to source32 9cc1dc0e; production reference authentication confirms pinned source objects, complete runtime bindings and target configs. Existing reference/package tests14/14 pass, source hashes unchanged during checks. This does not turn the native18/25 and24/25 failures into passes. ncnn remains clean at6a1bf000; no math/default changes or public push/release. Remaining geometry matrix, late-step drift, broad quality, native Windows/macOS, formal peer S/M and overall surpass goal stay open.

## 2026-09-07 — Shared 768 step6 replay and input isolation complete

- Previous goal turn made progress through commit36973c2 and the completed square-reference registration. Current turn extends the existing block-sequence probe, using ModelPackage for shared runtime geometry and actual text-token selection plus explicit threads. No duplicate inference math or per-shape exported models. CMake build succeeds, seven invalid commands reject before output, no-package syntax branch passes with configured Clang. Initial manual GCC/Clang-flag mismatch is preserved as a tooling failure, not a model failure.
- Frozen current334source bindings / probe1d754f49 / plan78910ae4 run the saved native768 prediction6 and exactly reproduce all294912 FP32 values. Teacher forcing the same step from saved official latent/conditioning passes unchanged NRMSE4.061437e-6/max0.000172257 versus oldmax0.060227075. The original free-running native distance was NRMSE0.0022942704/max0.235822916 and remains failed. Original generator92559ea4 remains separate from the new one-step probe.
- Follow-up two-factor plan was frozen after the replay and teacher results: only latent replaced gives descriptive NRMSE4.125540e-6/max0.000242591; only padded text replaced leaves NRMSE0.0022964643/max0.236498833. Five other inputs remain exact native bytes in each case. These hybrid inputs have no official gate and no native-generation acceptance eligibility. Evidence points to latent entering this step as the dominant conditional influence, not the source of the initial discrepancy; do not assign additive causal percentages or infer text is irrelevant upstream.
- Independent arithmetic recheck reproduces both native8/8 and official8/8 saved Euler updates exactly, all4718592 values, from each side's own sample/prediction and official FP32 sigma differences. It narrows the current diagnosis away from Euler-state-update arithmetic; earlier DiT/text/time/position contributions and growth still require evidence. No inference mathematics or default precision changed.
- Four single-prediction commands return0; unchanged limits and max/OOM/OOM-kill0 throughout. WholeGPU peaks2812/2807/2808/2808MiB; ownedcompute1127MiB each. Cgroup peaks include file cache; single-prediction scopes do not support S/M. Unified82828,57358,13734 all terminal and factor master PID absent. Evidence archived at artifacts/2026-09-07/shared-step768; raw local records under /var/tmp/ernie-step768-v1. No unchanged model reruns, no push/release, ncnn clean/pinned. Original full76818/25 and all broader gaps remain; goal active.

## 2026-09-07 — Shared 768 first-prediction CLI and comparison complete

- The preceding user-status reply only reread memory-policy code; it made no new implementation or model progress. This continuation verified that all earlier jobs were terminal, then extended the existing Python single-step command to authenticate shared packages and complete references, infer runtime geometry/text bucket, and pass explicit threads/host weights to the existing native probe. Fixed-package compatibility remains. The time-feature utility now accepts step0..7, preserving the default step6 output exactly. No inference math/default changes.
- Twenty relevant Python checks pass (6 new plus14 existing), and the actual feature utility build/3 valid/3 invalid calls pass. Preserve the initial test-service PYTHONPATH import failure; the corrected invocation supplies the environment inside the service. Final source inventory has335 files. The independent audit initially used one wrong parent directory for ncnn; its original script/log remain and the path-only correction passes without any model rerun.
- Plan d223454f05f387e336e5b37718a61c13b9dcf8339fab5671ceb760c1a5a7d5df freezes385 bindings and the unchanged1d754f49 native probe. First native replay exactly matches old768 prediction0 on294912 finite floats, distance to official NRMSE3.13661213e-5/max0.00120186806. Only after that check, the actual new shared-package Python CLI consumes official-prepared inputs and gives NRMSE4.65636934e-5/max0.00320696831, within the old step0 max0.067200556/NRMSE0.003 gates. Both calls use VulkanFP32/host/stdio/cache0/threads2/source32/15valid/64DiTslots/36blocks+heads.
- Official-prepared input substitution did not improve the first-prediction discrepancy. This does not establish that native conditioning is more accurate; computational differences, including CPU/CUDA trigonometric last bits, remain. Earlier step6 latent sensitivity plus this observation motivates aligned-input DiT stage comparison, not another unchanged step6 run or blindly replacing native text/time preparation. Original full76818/25 and all historical failures remain.
- Both model commands/workers/supervisors exit0; max/OOM/OOM-kill0. Native replay scope86.951188s/cgrouppeak12174524416B/wholeGPU2811MiB; official-input CLI scope102.098649s/cgrouppeak2891300864B/wholeGPU2810MiB; ownedcompute1127MiB each. The latter includes Python preparation and an extra package check; these are not paired speed/RSS or auto/OOM recovery measurements. Sessions75541/42560 terminal0, no jobs remain. Independent comparisons reread385 bindings/raw outputs; final audit matches335 current sources,112 copied Python sources, official selected inputs/expected bytes and source/target authentication. ncnn clean/pinned6a1bf000.
- Evidence: artifacts/2026-09-07/shared-step0-768; local raw records /var/tmp/ernie-step0-768-v1. Broad Q/F/S/M/D/A acceptance and complete surpass objective remain active. No push/release; no unchanged model reruns.

## 2026-09-07 — Shared 768 first-prediction stage trace complete

- Extended the existing diagnose_dit_stages.py to use authenticated shared-package selection and the existing reference_package helper, with explicit official root, threads and RAM-weight request. Source32 official provenance uses the registered source64 donor only after CAS/revision/all71nongraph asset equality checks. Each of36 official blocks and6heads is checked by the existing loaders; actual hashes are retained. Five new tests plus20 related existing checks pass. No native source, precision, inference math or defaults changed.
- Actual top-level shared-package command executes official CPU heads/CUDA streamed blocks first and native Vulkan only after the official subprocess exits. Frozen336 sources,419bindings, plan863167a7, unchanged probe1d754f49, source32/15IDs/64DiTslots/48x48packed/FP32/host/stdio/cache0/threads2. All six prepared inputs equal the prior teacher fixture. Official and native predictions each replay their own saved anchors byte-for-byte, confirming tracing did not change these outputs.
- Independent NumPy audit recomputes44 stages and358903808 finite values per side, including image/text partitions, agreeing with PyTorch metrics. Largest relative growth is block17/16=2.10458,18/17=2.08532,19/18=2.07178 (zero based); trajectory growth includes both incoming error and local calculation and does not identify a faulty operator. Final prediction NRMSE4.656369e-5/max0.003206968 remains within the old step0 limits. Original full76818/25 stays failed; CPU/CUDA trigonometric differences remain included. Next useful isolation is same-input evaluation of a selected block, not another unchanged full step.
- Whole stage wrapper returns0, official subprocess checked successful, native result0; max/OOM/OOM-kill0. Scope164.956536s/cgrouppeak14278266880B/wholeGPU3297MiB/ownedcompute1670MiB includes both backends and trace, not native performance or allocator-complete residency. Existing experimental resource controls unchanged. Test66411,prepare14618,model5793,audit86316 all terminal; actual process inspection shows no remaining model jobs. Archive source patch reconstructs all336 sources fromd001edc, copied112 Python files and419 bindings remain valid, ncnn clean/pinned.
- Evidence archived at artifacts/2026-09-07/shared-stages0-768; raw tensors/binaries/samples remain local under /var/tmp/ernie-stages0-768-v1. Current user memory-policy question was answered from live code: default auto selects RAM for estimated DiT weight pressure; optional cache checks RAM headroom, activations/workspace and failed-command recovery remain unsupported. The existing controlled512 RAM test is not actual GPU exhaustion. No new memory-policy changes or repeated full-generation job for that question. Broader quality/shapes/platforms/formal peer S/M and full surpass goal remain open; no push/release.

## 2026-09-07 — Shared first-step blocks17/18/19 same-input isolation complete

- Previous goal turn made authoritative progress through local commit77085eb, preserving the44stage result and existing memory-policy answer. This turn extends the existing block-sequence probe with authenticated shared-package --block-index0..35, ten raw block inputs and global trace numbering. All computation still uses production run_block_sequence; fixed raw and full shared DiT modes remain. Build session2072 returns0;10 actual invalid argument calls reject before file access/output and configured Clang no-package-macro syntax succeeds.
- Planf5a8b08f freezes336sources/390bindings and newprobe88aaf821. Source32/768/15IDs/64DiTslots/2368tokens/FP32/threads2/host/stdio/cache0. Position preparation evaluates only frequency cos/sin onCUDA and saves the tables; no model forward. Three selected official forwards exactly reproduce old official stages, and all three native replay controls exactly reproduce saved native stages before six teacher calls. No full image/text/heads/Euler/VAE rerun.
- At zero-based blocks17/18/19, fully aligned hidden/modulation/mask/CUDA positions yield NRMSE7.136946e-7/7.427295e-7/8.391165e-7 and maxima0.00634765625/0.004638671875/0.008544921875. Original cumulative NRMSE9.787622e-6/2.041035e-5/4.228584e-5 is13.714/27.480/50.393 times larger. This supports propagated-input effects in this fixture; it does not assign causal percentages, identify initial root cause, or establish a defective block. Swapping only CPU/CUDA position tables does not consistently lower distance to official, so no production position change is justified here.
- Independent NumPy FP64 audit verifies all390bindings/current336sources, all87293952 native output values and29097984 official values finite, and all metrics match PyTorch. Cos/sin differ in64014/43154 of303104values each, max5.96046e-8. Official/native model scopes9.797809/195.886658s, cgrouppeak1716371456/11895750656B includes cache, wholeGPU2666/2739MiB, ownedcompute1670/1044MiB; both commands0/maxOOM0. Prepare service0/25.630s/reported4Gpeak had no retained event counters, so whole-chain max0 is not claimed. Audit0/6.983s/1.4Gpeak. Model59902/83028 and audit82289 terminal.
- Results and small records archived at artifacts/2026-09-07/shared-blocks17-19-768; raw tensors/binary/samples remain under /var/tmp/ernie-blocks17-19-768-v1. Source patch reconstructs from77085eb; ncnn clean/pinned. No generator math/precision/default/threshold change, no full76818/25 closure, no push/release. Further work must preserve these completed controls and focus on remaining actual geometries/full quality and platform/paired reference evidence, rather than repeat the same local calls or assume these three blocks are faulty. Overall goal remains active.

## 2026-09-07 — Four remaining normal-aspect large canaries frozen

- Prepared 1376x768,768x1376,2048x1024,1024x2048 native/official full 8-step cases with saved same-noise inputs per geometry. Reuses original Linux binary92559ea4/source990e8ef/334files and official helper snapshot; source32/15IDs/64DiTslots/FP32/host/stdio/cache0/CPU2/traceON. Current HEAD0443a6d differs in native src/include/cli only by Windows memory reader and header comments; complete lineage.patch retained. Not a current-HEAD build or repeated square canary.
- Each case binds436 source/input/binary/metadata files; all1744 checks pass. Actual source hiding, offline namespaces and native-help checks give17 successful preflight items. Thirteen synthetic policy tests verify malformed/incomplete/contradictory comparisons stop, valid numeric failure continues to next independent case with final exit1, and execution failure stops before the next case. All test/preparation/preflight services return0; no model runs were needed for these checks. Original thresholds and 16GiB/noSwap/CPU2/3GiBhost/6144MiBwholeGPU/1800sec controls remain. These controls are assistant-selected experiment settings, not official hardware minima.
- Small frozen protocol archived at artifacts/2026-09-07/runtime-large-protocol; local inputs/binary/source/helper and future outputs under /var/tmp/ernie-runtime-large-v1. Protocol is preparation, not four completed images. Read progress.json plus actual run_batch/supervisor/native processes before any launch or continuation; never overwrite/retry an existing run. Extreme16x16/16x2048/2048x16, broad quality and formal peer/platform/whole-goal acceptance remain open. No production inference math/default changes or push/release.
