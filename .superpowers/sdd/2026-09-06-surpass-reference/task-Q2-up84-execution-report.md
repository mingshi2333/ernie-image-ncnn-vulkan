# Q2 up84 actual attempt — execution identity failure

The uniquely authorized frozen launcher/plan were externally authenticated and executed once (session65910). The dedicated scope was ernie-q2-up84-v1.scope. At 6.515059s the guard rejected actual mapped `/usr/lib64/amdvlk64.so`, absent from the frozen allowlist, and killed the child (-9); launcher exited1. All experiment processes exited and GPU was immediately released. No retry, bypass, backend selection, production math or gate change occurred.

Native runner PID2183104 mapped the library while remaining in the expected scope with affinity0,2. No native tensors were saved and native.log is empty. The official MLP was never launched. There is no complete84 denominator, no finite/full-output invariant evidence and no numerical result to interpret. Calling this a quality failure or improvement would be incorrect.

All13 saved resource samples have zero process swap and zero memory events, expected cgroup and affinity. Peak recursiveRSS515002368B; sampled wholeGPU1699MiB; sampled cgroup memory.current654340096B; minimum hostavailable16989167616B. These measure different quantities. The actual preflight required memory.max10GiB, swap.max0 and CPU200%; guard and original thresholds remained unchanged. Successful creation of attempt.json followed the full6133bound check, but did not imply completeness of dynamically loaded runtime identity.

Machine-readable independent CPU audit is outputs/q2-up84-v1-post-audit/audit.json, with SHA bindings to actual plan, launcher, attempted worker, samples and logs. Original v1 files are preserved, including worker-result.json status guard_or_child_failure; the post-audit classifies this as execution_identity_failed.

## CPU root cause and correction required before any new attempt

The actual frozen `execution/freeze-initial-plan.py:23` enumerated only `/usr/share/vulkan/icd.d/*.x86_64.json`; catalog wording claiming all installed64-bit ICDs was too broad. `/etc/vulkan/icd.d/amd_icd64.json` and `/etc/vulkan/implicit_layer.d/amd_icd64.json` both contain library_path `/usr/lib64/amdvlk64.so`. They are byte-identical. Neither manifest nor the library was bound. Thus there are two independent discovery omissions: a system configuration directory, and a valid filename not ending `.x86_64.json`. Actual samples prove the mapped library, but cannot distinguish ICD discovery from implicit-layer discovery; no loader debug trace was captured. Do not invent that causal distinction.

A replacement catalog must enumerate the loader configuration/data roots and applicable environment overrides, use all JSON manifests rather than filename architecture assumptions, resolve absolute/relative/basename library paths correctly, check ELF architecture, and collect the dependency closure for every resolved applicable ICD/layer library. Record directory membership and environment so changes failclosed. Actual mapped-library checks must remain unchanged and unresolved entries must remain explicit; do not merely append one AMD SHA. A new attempt needs a new output and independent preparation review. No additional model or GPU execution is authorized by this report.
