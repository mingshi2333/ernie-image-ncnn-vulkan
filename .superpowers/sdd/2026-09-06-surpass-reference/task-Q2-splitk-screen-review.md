# Q2 split-K 局部候选独立复核

审查对象：`4c35148` 的独立诊断实现与 `outputs/q2-down-splitk-v3`。未运行生产推理或修改默认数学；v1 GLSL 保留字失败、v2 上传 packing 错误的原始目录均保留。

root 重新计算 plan SHA 和全部 48 个绑定文件 SHA，以及两份实际 GPU 输出 SHA，均匹配。独立读取固定官方 safetensors 的 BF16 down 权重，逐块确认其 FP32 展开/转置与 fixture 的 [K,N] 权重全量相同，再用 FP64 点积重算两组各五行的全部 4096 输出列；与保存 oracle 在 1e-12 相对/绝对数值范围内一致。使用 FP64 差值重新计算全部 20480 元素的每组 L2/max：

| 条件输入 | 原始 L2 | 候选 L2 | 原始 max | 候选 max |
|---|---:|---:|---:|---:|
| official block14 | 0.00281606515958 | 0.00003764513360 | 0.00022600889085 | 0.00000683466953 |
| native block14 | 0.00280094317568 | 0.00003659472710 | 0.00021668651242 | 0.00000333609901 |

两组分别满足事先固定的 L2/max 至少两倍改善门槛。另重新计算独立 synthetic 输入/权重的 FP64 点积；全部 4×4096 实际 GPU 输出与 FP64 四舍五入到 FP32 后逐位相同。原始独立复核 JSON 位于 `outputs/q2-down-splitk-review-v3/result.json`。

代码限定固定 K=12288、N=4096、M≤10。flat 上传与 elempack=1/h=1 断言关闭已观察到的 packing 错误；32 段完整覆盖 K，每段保留 sum/correction 两个 FP32 分量，合并时读取两者。没有替换 ncnn 原始依赖或注册生产层。独立抵消/范围 fixture 有有限的非零模式，其余列为零；通过这些反例不等于证明任意输入的误差界或其他设备数学行为。

此结果允许进入完整 block15 teacher 诊断。它仅证明选定真实行的局部算子误差改善，不证明所有 4160 行、完整 block、单步、中文自由轨迹或正式 Q 门槛改善；也不是性能加速证据。本次局部切片没有未关闭的 Important finding。
