add_test(NAME native_cache_cpu COMMAND ernie-attention-probe --backend cpu)
add_test(NAME runtime_cache_cpu COMMAND ernie-attention-probe --backend cpu --runtime-layer)
if(ERNIE_ENABLE_VULKAN)
    add_test(NAME native_cache_vulkan COMMAND ernie-attention-probe --backend vulkan)
    add_test(NAME runtime_cache_vulkan COMMAND ernie-attention-probe --backend vulkan --runtime-layer)
    add_test(NAME attention_long_fp32_accuracy COMMAND ernie-attention-accuracy-probe)
    add_test(NAME attention_bounded_workspace COMMAND ernie-attention-workspace-probe)
    set_tests_properties(native_cache_vulkan runtime_cache_vulkan attention_long_fp32_accuracy attention_bounded_workspace
                         PROPERTIES SKIP_RETURN_CODE 77)
endif()
if(ERNIE_BUILD_GENERATOR)
    find_package(PNG REQUIRED)
    add_executable(ernie-utf8-paths-contract test_utf8_paths.cpp
                   "${PROJECT_SOURCE_DIR}/cli/options.cpp" "${PROJECT_SOURCE_DIR}/cli/prompt_file.cpp")
    target_include_directories(ernie-utf8-paths-contract PRIVATE "${PROJECT_SOURCE_DIR}/cli")
    target_link_libraries(ernie-utf8-paths-contract PRIVATE ernie::pipeline ernie-model-package ernie-runtime)
    add_test(NAME utf8_paths_contract COMMAND ernie-utf8-paths-contract)
    add_executable(ernie-generation-report-contract test_generation_report.cpp "${PROJECT_SOURCE_DIR}/cli/generation_report.cpp")
    target_include_directories(ernie-generation-report-contract PRIVATE "${PROJECT_SOURCE_DIR}/include" "${PROJECT_SOURCE_DIR}/cli")
    add_test(NAME generation_report_writer COMMAND ernie-generation-report-contract)
    add_executable(ernie-pipeline-api-contract test_pipeline_api.cpp)
    target_link_libraries(ernie-pipeline-api-contract PRIVATE ernie::pipeline)
    add_test(NAME pipeline_api_contract COMMAND ernie-pipeline-api-contract)
    add_executable(ernie-request-validation-contract test_request_validation.cpp)
    target_link_libraries(ernie-request-validation-contract PRIVATE ernie::pipeline)
    add_test(NAME request_validation_contract COMMAND ernie-request-validation-contract)
    find_package(Threads REQUIRED)
    add_executable(ernie-gpu-context-contract test_gpu_context.cpp)
    target_link_libraries(ernie-gpu-context-contract PRIVATE ernie::pipeline ernie-runtime Threads::Threads)
    add_test(NAME gpu_context_contract COMMAND ernie-gpu-context-contract)
    set_tests_properties(gpu_context_contract PROPERTIES SKIP_RETURN_CODE 77)
    if(ERNIE_ENABLE_VULKAN)
        add_test(NAME gpu_context_missing_driver COMMAND ernie-gpu-context-contract --without-driver)
        set_tests_properties(gpu_context_missing_driver PROPERTIES ENVIRONMENT
            "VK_DRIVER_FILES=${CMAKE_CURRENT_BINARY_DIR}/absent-vulkan-driver.json;VK_ICD_FILENAMES=${CMAKE_CURRENT_BINARY_DIR}/absent-vulkan-driver.json")
    endif()
    add_executable(ernie-prompt-file-contract test_prompt_file.cpp "${PROJECT_SOURCE_DIR}/cli/prompt_file.cpp")
    target_include_directories(ernie-prompt-file-contract PRIVATE "${PROJECT_SOURCE_DIR}/cli")
    add_test(NAME prompt_file_contract COMMAND ernie-prompt-file-contract)
    add_executable(ernie-image-io-contract test_image_io.cpp "${PROJECT_SOURCE_DIR}/cli/image_io.cpp")
    target_include_directories(ernie-image-io-contract PRIVATE "${PROJECT_SOURCE_DIR}/include"
                               "${PROJECT_SOURCE_DIR}/cli"
                               "${PROJECT_SOURCE_DIR}/third_party/stb")
    target_link_libraries(ernie-image-io-contract PRIVATE PNG::PNG)
    add_test(NAME image_io_contract COMMAND ernie-image-io-contract)
    add_executable(ernie-pe-sampling-contract test_pe_sampling.cpp)
    target_link_libraries(ernie-pe-sampling-contract PRIVATE ernie-pe)
    add_test(NAME pe_sampling_contract COMMAND ernie-pe-sampling-contract)
    find_package(Python3 COMPONENTS Interpreter QUIET)
    if(Python3_Interpreter_FOUND)
        add_test(NAME generation_report_json COMMAND "${Python3_EXECUTABLE}"
                 -m unittest discover -s "${CMAKE_CURRENT_SOURCE_DIR}" -p test_generation_report.py -v)
        set_tests_properties(generation_report_json PROPERTIES ENVIRONMENT "ERNIE_REPORT_RUNNER=$<TARGET_FILE:ernie-generation-report-contract>")
        add_test(NAME pe_package_integrity COMMAND "${Python3_EXECUTABLE}"
                 -m unittest discover -s "${CMAKE_CURRENT_SOURCE_DIR}" -p test_pe_package.py -v)
        set_tests_properties(pe_package_integrity PROPERTIES ENVIRONMENT "ERNIE_TEST_RUNNER=$<TARGET_FILE:ernie-image>")
        add_test(NAME package_integrity COMMAND "${Python3_EXECUTABLE}"
                 -m unittest discover -s "${CMAKE_CURRENT_SOURCE_DIR}" -p test_package.py -v)
        set_tests_properties(package_integrity PROPERTIES ENVIRONMENT "ERNIE_TEST_RUNNER=$<TARGET_FILE:ernie-image>")
        add_test(NAME cli_contract COMMAND "${Python3_EXECUTABLE}"
                 -m unittest discover -s "${CMAKE_CURRENT_SOURCE_DIR}" -p test_cli.py -v)
        set_tests_properties(cli_contract PROPERTIES ENVIRONMENT "ERNIE_TEST_RUNNER=$<TARGET_FILE:ernie-image>")
        if(ERNIE_INSTALL_SDK)
            add_test(NAME installed_cpp_consumer COMMAND "${Python3_EXECUTABLE}"
                -m unittest discover -s "${CMAKE_CURRENT_SOURCE_DIR}" -p test_install.py -v)
            set_tests_properties(installed_cpp_consumer PROPERTIES
                ENVIRONMENT "ERNIE_INSTALL_BUILD=${CMAKE_BINARY_DIR};ERNIE_INSTALL_CONFIG=$<CONFIG>"
                RUN_SERIAL TRUE TIMEOUT 900)
        endif()
        add_test(NAME text_bucket_contract COMMAND "${Python3_EXECUTABLE}"
                 -m unittest discover -s "${CMAKE_CURRENT_SOURCE_DIR}" -p test_text_bucket.py -v)
        add_test(NAME variant_dimensions COMMAND "${Python3_EXECUTABLE}"
                 -m unittest discover -s "${CMAKE_CURRENT_SOURCE_DIR}" -p test_variant.py -v)
    endif()
endif()
add_executable(ernie-latent-contract test_latent_contract.cpp)
target_link_libraries(ernie-latent-contract PRIVATE ernie-runtime)
add_test(NAME latent_contract_cpu COMMAND ernie-latent-contract)
add_executable(ernie-component-files-contract test_component_files.cpp)
target_link_libraries(ernie-component-files-contract PRIVATE ernie-runtime)
add_test(NAME component_files_contract_cpu COMMAND ernie-component-files-contract)
add_executable(ernie-weight-placement-contract test_weight_placement.cpp)
target_link_libraries(ernie-weight-placement-contract PRIVATE ernie-runtime)
add_test(NAME weight_placement_cpu COMMAND ernie-weight-placement-contract)
add_executable(ernie-weight-session-contract test_weight_session.cpp)
target_link_libraries(ernie-weight-session-contract PRIVATE ernie-runtime)
add_test(NAME weight_session_cpu COMMAND ernie-weight-session-contract)
add_executable(ernie-host-memory-contract test_host_memory.cpp)
target_link_libraries(ernie-host-memory-contract PRIVATE ernie-runtime)
add_test(NAME host_memory_cpu COMMAND ernie-host-memory-contract)
add_executable(ernie-model-loading-contract test_model_loading.cpp)
target_link_libraries(ernie-model-loading-contract PRIVATE ernie-runtime)
add_test(NAME model_loading_cpu COMMAND ernie-model-loading-contract)
add_executable(ernie-model-reader-contract test_model_reader.cpp)
target_link_libraries(ernie-model-reader-contract PRIVATE ncnn)
target_compile_definitions(ernie-model-reader-contract PRIVATE
    ERNIE_COMPACT_MODEL_READER_TEST=$<BOOL:${ERNIE_EXPERIMENT_COMPACT_MODEL_READER}>)
add_test(NAME model_reader_cpu COMMAND ernie-model-reader-contract)
if(ERNIE_ENABLE_VULKAN)
    add_test(NAME weight_session_vulkan COMMAND ernie-weight-session-contract vulkan)
    set_tests_properties(weight_session_vulkan PROPERTIES SKIP_RETURN_CODE 77)
    add_test(NAME weight_session_mapped_vulkan COMMAND ernie-weight-session-contract vulkan mapped)
    add_test(NAME weight_session_bf16_file_vulkan COMMAND ernie-weight-session-contract vulkan stdio bf16)
    add_test(NAME weight_session_mapped_bf16_file_vulkan COMMAND ernie-weight-session-contract vulkan mapped bf16)
    set_tests_properties(weight_session_mapped_vulkan weight_session_bf16_file_vulkan
                        weight_session_mapped_bf16_file_vulkan PROPERTIES SKIP_RETURN_CODE 77)
    add_test(NAME weight_placement_vulkan COMMAND ernie-weight-placement-contract vulkan)
    set_tests_properties(weight_placement_vulkan PROPERTIES SKIP_RETURN_CODE 77)
endif()
add_executable(ernie-img2img-contract test_img2img_contract.cpp)
target_link_libraries(ernie-img2img-contract PRIVATE ernie-runtime)
add_test(NAME img2img_contract_cpu COMMAND ernie-img2img-contract)
add_executable(ernie-image-encoder-contract test_image_encoder.cpp)
target_link_libraries(ernie-image-encoder-contract PRIVATE ernie-runtime)
add_test(NAME image_encoder_contract_cpu COMMAND ernie-image-encoder-contract)
add_executable(ernie-residual-contract test_residual_contract.cpp)
target_link_libraries(ernie-residual-contract PRIVATE ernie-runtime)
add_test(NAME residual_contract_cpu COMMAND ernie-residual-contract)
if(ERNIE_ENABLE_VULKAN)
    add_test(NAME residual_contract_vulkan COMMAND ernie-residual-contract vulkan)
    set_tests_properties(residual_contract_vulkan PROPERTIES SKIP_RETURN_CODE 77)
    add_test(NAME residual_contract_vulkan_bf16 COMMAND ernie-residual-contract vulkan bf16)
    set_tests_properties(residual_contract_vulkan_bf16 PROPERTIES SKIP_RETURN_CODE 77)
endif()
add_test(NAME rmsnorm_range_cpu COMMAND ernie-rmsnorm-probe cpu fp32)
add_test(NAME layernorm_range_cpu COMMAND ernie-rmsnorm-probe cpu fp32 --layernorm)
add_test(NAME erf_gelu_cpu COMMAND ernie-gelu-probe cpu fp32)
if(ERNIE_ENABLE_VULKAN)
    foreach(precision fp32 fp16 bf16)
        add_test(NAME erf_gelu_vulkan_${precision} COMMAND ernie-gelu-probe vulkan ${precision})
        set_tests_properties(erf_gelu_vulkan_${precision} PROPERTIES SKIP_RETURN_CODE 77)
        add_test(NAME rmsnorm_range_vulkan_${precision} COMMAND ernie-rmsnorm-probe vulkan ${precision})
        set_tests_properties(rmsnorm_range_vulkan_${precision} PROPERTIES SKIP_RETURN_CODE 77)
        add_test(NAME layernorm_range_vulkan_${precision} COMMAND ernie-rmsnorm-probe vulkan ${precision} --layernorm)
        set_tests_properties(layernorm_range_vulkan_${precision} PROPERTIES SKIP_RETURN_CODE 77)
    endforeach()
endif()

add_executable(ernie-shape-plan-contract test_shape_plan.cpp)
target_link_libraries(ernie-shape-plan-contract PRIVATE ernie-shape-plan)
add_test(NAME shape_plan_contract COMMAND ernie-shape-plan-contract)

if(ERNIE_BUILD_TOKENIZER)
    add_executable(ernie-shape-graph-contract test_shape_graph.cpp)
    target_link_libraries(ernie-shape-graph-contract PRIVATE ernie-shape-graph)
    add_test(NAME shape_graph_contract COMMAND ernie-shape-graph-contract)
endif()

add_executable(ernie-text-down-contract test_text_down.cpp)
target_link_libraries(ernie-text-down-contract PRIVATE ernie-runtime)
target_compile_definitions(ernie-text-down-contract PRIVATE ERNIE_TEXT_DOWN_FIXTURE="${CMAKE_CURRENT_SOURCE_DIR}/fixtures/text-down-s64.param")
add_test(NAME text_down_contract_cpu COMMAND ernie-text-down-contract)

if(ERNIE_BUILD_TOKENIZER)
    add_executable(ernie-model-package-contract test_model_package.cpp)
    target_link_libraries(ernie-model-package-contract PRIVATE ernie-model-package)
    add_test(NAME model_package_contract COMMAND ernie-model-package-contract)
endif()

find_package(Threads REQUIRED)
add_executable(ernie-execution-metrics-contract test_execution_metrics.cpp)
target_link_libraries(ernie-execution-metrics-contract PRIVATE ernie-execution-metrics Threads::Threads)
add_test(NAME execution_metrics_cpu COMMAND ernie-execution-metrics-contract)

if(ERNIE_ENABLE_ALLOCATION_METRICS)
    add_executable(ernie-allocation-hooks-contract test_allocation_metrics_hook.cpp)
    target_compile_definitions(ernie-allocation-hooks-contract PRIVATE ERNIE_ALLOCATION_HOOKS_ENABLED=1)
    target_link_libraries(ernie-allocation-hooks-contract PRIVATE ernie-allocation-metrics)
    add_test(NAME allocation_hooks_cpu COMMAND ernie-allocation-hooks-contract)
endif()

# Explicit diagnostic target; OFF does not link observer code or export ncnn.
option(ERNIE_BUILD_ALLOCATION_PROBE "Build the bounded actual Vulkan allocation contract" OFF)
if(ERNIE_BUILD_ALLOCATION_PROBE AND ERNIE_ENABLE_VULKAN)
    add_executable(ernie-allocation-vulkan-contract test_allocation_metrics_vulkan.cpp)
    target_link_libraries(ernie-allocation-vulkan-contract PRIVATE ncnn)
    if(ERNIE_ENABLE_ALLOCATION_METRICS)
        target_compile_definitions(ernie-allocation-vulkan-contract PRIVATE ERNIE_REAL_ALLOCATION_HOOKS=1)
        target_link_libraries(ernie-allocation-vulkan-contract PRIVATE ernie-allocation-metrics)
    endif()
endif()

add_executable(ernie-pe-prefill-contract test_pe_prefill.cpp)
target_link_libraries(ernie-pe-prefill-contract PRIVATE ernie-runtime)
target_compile_definitions(ernie-pe-prefill-contract PRIVATE
    ERNIE_PE_GRAPH_FIXTURE="${CMAKE_CURRENT_SOURCE_DIR}/fixtures/pe-canonical.param")
add_test(NAME pe_prefill_cpu COMMAND ernie-pe-prefill-contract)

add_executable(ernie-vulkan-memory-contract test_vulkan_memory.cpp)
target_link_libraries(ernie-vulkan-memory-contract PRIVATE ernie-runtime)
add_test(NAME vulkan_memory_errors_cpu COMMAND ernie-vulkan-memory-contract)
if(ERNIE_ENABLE_VULKAN)
    add_test(NAME vulkan_memory_buffers COMMAND ernie-vulkan-memory-contract vulkan)
    set_tests_properties(vulkan_memory_buffers PROPERTIES SKIP_RETURN_CODE 77)
    add_executable(ernie-memory-execution-contract test_memory_execution.cpp)
    target_link_libraries(ernie-memory-execution-contract PRIVATE ernie-runtime)
    add_executable(ernie-ncnn-failure-contract "${PROJECT_SOURCE_DIR}/cmake/test_ncnn_failures.cpp")
    target_link_libraries(ernie-ncnn-failure-contract PRIVATE ncnn Threads::Threads)
    add_test(NAME ncnn_failure_vulkan COMMAND ernie-ncnn-failure-contract)
    set_tests_properties(ncnn_failure_vulkan PROPERTIES SKIP_RETURN_CODE 77)
    add_test(NAME memory_execution_vulkan COMMAND ernie-memory-execution-contract)
    set_tests_properties(memory_execution_vulkan PROPERTIES SKIP_RETURN_CODE 77)
endif()
include("${PROJECT_SOURCE_DIR}/cmake/ErnieVulkanValidation.cmake")
ernie_enable_vulkan_validation()
