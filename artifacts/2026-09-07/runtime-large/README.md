# 四种大尺寸完整对照：已启动，等待结果

四个样例的参数、程序和停止规则已在[冻结协议](../runtime-large-protocol/README.md)及本地提交 `99e9fa8` 中保存，随后才启动执行。`launch.json` 是启动后的时间点快照，不是持续更新的状态，也不代表全部完成。

统一执行会话为 `56706`，主进程 `3345676`。记录快照时正在执行 `1376×768` 原生阶段：实际生成器 PID `3345721`，监督进程 PID `3345712`；原生 15-token 文本编码和至少第 1/8 步去噪完成。该时间点 cgroup 的 max/OOM/OOM-kill 均为 0。官方阶段及另外三个尺寸尚未开始，不能从这个快照得出全程资源结论。

当前状态读取 `/var/tmp/ernie-runtime-large-v1/progress.json`，再核对实际主进程/监督进程/生成器和每阶段 `process.json`。现有批次会依冻结顺序继续；禁止另起同样批次、覆盖输出或并行执行 GPU 模型。有效数值未通过只保留结果并继续下个尺寸，执行/资源/无效比较失败会停止，批次不会自动重试。

`review_case.py` 是由旧方图独立复核脚本适配目标几何的只读复核器，按给定 case 名检查全套冻结绑定、实际命令/报告、25 个有限张量以及 PNG；新增检查要求全部误差值与原比较一致。已完成语法检查，此时没有对不完整输出执行。它不改变生成和原比较器。

后续补充实际完成记录后才能声明相应尺寸通过执行或数值检查。其他质量、极端尺寸、平台及正式参考项目比较仍未完成。没有修改推理数学、默认值或旧门槛，没有公开推送或发布。
