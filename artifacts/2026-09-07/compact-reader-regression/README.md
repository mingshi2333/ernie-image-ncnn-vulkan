# Compact ModelBin 读取器：完整 512 回归通过

2026-09-07。默认关闭的紧凑读取缓冲候选完成一次完整生成：原生文本、8 步 Vulkan FP32 和 CPU direct VAE 全部执行，25 个有限张量、4,981,760 个数值与已验证 RAM 基线逐字节相同，PNG 也逐字节相同。max/OOM/OOM-kill 为 0，原生进程成功退出、任务进程组已消失。

## 候选与执行

候选只修正固定 ncnn 普通 FP16/BF16 读取路径的两个临时数组元素计数，在构建目录生成经过源码散列检查的副本，未改动 ncnn 工作区。先前实际 ModelBin 分配检查显示：8,388,612 字节载荷的临时数组由 16,777,224 字节降为 8,388,612 字节，详见[算子和构建验证](../compact-model-reader/README.md)。这不等于整个程序 RAM 减半。

[本次协议](../compact-reader-regression-protocol/README.md)在运行前冻结。计划 SHA256 为 `45d43302b30d1e91f2bcfd9eeda6cc79935bd0e59d08c972569e7a59f389fae2`，程序 SHA256 为 `b15ccce8e92962b041df6581926dd7c06f1f75b65b1b2cbb949f927be71ca05a`，来源 `8158dc1`。后来 Windows/UTF-8 改动不包含在该快照中。

同一 shared source32 模型、15 个原生 token、64 个 DiT 文本槽、已保存初始 latent，512×512、8 步 FP32、CPU Vector 文本、CPU direct VAE、PE 关闭、2 线程。显式 stdio、RAM 权重、缓存 0，张量 trace 打开。374 个绑定以及实际命令、报告、输入和候选身份由冻结比较器核对。

该任务只在原有 16 次内存对照全部完成且主进程已退出后启动；前期仍在运行时的拒绝记录保留在协议中。源码/输入只读，隐藏原仓库、禁网，仍使用原先 16 GiB 进程组、交换配额 0、主机可用 RAM 至少 3 GiB、整卡 6144 MiB 和 1800 秒保护设置。没有并行运行第二个模型。

## 结果

| 项目 | 实际结果 |
|---|---|
| 原生进程 | 返回 0，完整结束 |
| 中间结果 | 25/25 张量，共 4,981,760 个值；全部有限、逐字节相同、最大绝对差 0 |
| 输入核对 | 初始 latent、文本、token ID 和提示词与冻结基线一致 |
| PNG | 像素和文件字节均相同，最大通道差 0 |
| 权重放置请求 | GPU 0、RAM 304、预算不可用 0；缓存全部统计为 0 |
| 内存事件 | max、OOM、OOM-kill 均为 0 |
| 进程组采样峰值 | 2,817,376,256 字节，包含文件缓存 |
| 原生 VmHWM 采样峰值 | 2,085,281,792 字节；VmSwap 样本为 0 |
| 整卡显存采样峰值 | 2308 MiB，包含桌面等其他进程 |
| 外层总时间 | 443.709 秒，包含外层准备和张量记录；不是纯原生生成时间 |

PNG SHA256 为 `bf87214d21461bfda96529830b28a5b7630ef0dcf307c0172f1666fc64044e28`。日志没有出现 ncnn 权重 allocator 回落到设备内存的信息；日志缺少该消息本身不是完整分配器驻留证明。

完整结果见 `comparison.json`，根代理终态复核见 `root-review.json`；不是另一名独立评审。每阶段原始输出、实际命令、报告和进程记录已经归档。模型、程序、PNG、原始采样和张量仍保存在 `/var/tmp/ernie-compact-reader512-v1`，散列/大小见 `retained-local-files.json`，不进入 Git。

## 结论边界

本次补充了候选在一个完整 512 案例中的结果一致性证据。它打开 trace，旧四配置对照关闭 trace，且使用不同程序、未控制同一页缓存状态，因此不能拿 443.709 秒与旧表直接比较并声称加速，也不能用两次不同范围的内存峰值计算优化比例。

`ERNIE_EXPERIMENT_COMPACT_MODEL_READER` 继续默认关闭。本次不增加真实显存耗尽、激活转移、OOM 恢复、正式速度/内存优势或新的官方质量样本。固定普通读取临时数组的减半已有直接分配证据；整个进程、更多尺寸和设备仍需相应证据。所有历史数值失败和总体未完成项保持原样。没有推送或发布。
