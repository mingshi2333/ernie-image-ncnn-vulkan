# 768 与 1024 运行时尺寸的完整开发对照协议

2026-09-07，冻结当前 Linux Vulkan 程序与共享包的两组完整运行：768×768、1024×1024。每组先运行原生生成器，再运行官方分阶段 FP32 参考，最后核对 25 个完整轨迹张量和 PNG。本目录记录准备、隔离预检及首个原生任务的实际启动，尚无完整图像或通过结论。实际输出在 `/var/tmp/ernie-runtime-squares-v1`，不得把本协议当作执行结果。

- 当前源码 `990e8ef07591c2f7564d538462d51b8961263655`，334 个源文件只读快照；Linux 程序 SHA256 `92559ea493adabfb1a0b2fbf1d69e21ccb79d7c9f2dbd37cb25898d7654da1c0`。它包含 Windows 路径修改后的 Linux 重建，映射加载与 compact-reader 实验的构建默认值均为 OFF；不是旧内存网格或 compact-reader 候选程序。
- 同一共享包 `21b6bc8d…`，source32 `9cc1dc0e…`，实际原生文本必须参与，64 个 DiT 文本槽、8 步、PE 关闭、CPU Vector 文本、Vulkan FP32 DiT、CPU direct VAE。两边逐尺寸共享保存的 FP32 初始噪声；同整数 seed 本身不作为输入等价证据。
- 原生固定 `--dit-weights host --model-loading stdio --dit-cache-mib 0`，保持尺寸测试的权重策略一致。它验证 RAM 权重下的完整新尺寸，不验证自动预算选择、真实显存耗尽或 Vulkan 失败恢复。trace 和完成报告开启，本项不是正式性能或参考项目的配对比较。
- 每组冻结 430 个输入/源码/二进制/来源元数据绑定，运行前后核验。原生还执行共享包全文件校验；官方加载器分别校验实际权重文件与已绑定来源。768 计划 SHA256 为 `fd6d3c8618303a7fb403383e115f42ba6198f69f73d4bbf2e6a4f7fb9b90e90d`，1024 为 `9f0be7d03d60ee0cfe25bef419a6a02caa6a928956229cca9bb646e214d820da`。
- 原生/官方任务均禁网；原生隐藏原项目源码与模型目录，只读挂载同一模型包。四个实际隔离/启动预检退出 0，脚本语法和两组全部绑定校验通过。这不是完整模型测试结果。
- 大型任务串行。保持原有实验保护设置：CPU 2 核、cgroup 16 GiB、该 cgroup 禁用 swap、主机可用 RAM 至少 3 GiB、整卡采样最多 6144 MiB、每阶段 1800 秒。这些是本项目助手选择的实验设置，不是模型物理硬件要求。整卡值包含桌面；cgroup 含文件缓存，不能称作进程 RSS。
- 数值检查沿用原有历史容差，不因新结果调整。完整执行、数值未通过、资源保护终止、真实 OOM 分别记录。批次遇首个非零返回即停止；不得覆盖输出或通过重跑删除失败。源文件和输入保持冻结，后续独立继续须明确记录。

`launch.json` 保存实际主进程与统一会话。恢复工作前检查真实进程及结果，不按这里的历史启动状态重开任务。此前全部 512 内存网格、compact-reader 和横竖图结果保持；本项不关闭整个尺寸矩阵、广泛质量、平台或整体超过目标。无公开推送或发布。
