# Compact reader 完整模型回归协议：已准备，未执行

2026-09-07。此记录固定 `8158dc1` 的 compact ModelBin 候选程序与源快照，等待现有 16 次内存/加载比较结束后执行。**这里没有新的完整模型结果、速度或进程内存改善结果。** 当前 Windows 路径修复不包含在这份候选快照中。

候选 Vulkan 二进制 SHA256 为 `b15ccce8e92962b041df6581926dd7c06f1f75b65b1b2cbb949f927be71ca05a`，冻结计划 SHA256 为 `45d43302b30d1e91f2bcfd9eeda6cc79935bd0e59d08c972569e7a59f389fae2`。计划绑定 374 个输入/来源，使用同一 shared source32 模型、15-token 原生 Vector 文本、64 个 DiT 文本槽、已保存初始 latent、512×512、8 步 Vulkan FP32、CPU direct VAE、2 线程、PE 关闭。明确指定 stdio、RAM 权重、缓存 0；逐步 trace 打开，故不用于速度排名。

原有 16 GiB 进程组、禁 swap、主机至少 3 GiB 可用 RAM、整卡 6144 MiB 保护线和 1800 秒阶段超时保留。源码与大基线张量使用只读副本；运行时隐藏原项目并禁网。保护线是实验设置，不是硬件最低要求。

supervisor 在创建 native 输出或启动模型之前，要求旧 grid 的计划 SHA、16 个成功结果、终态汇总和已退出主进程全部满足。预检已验证全部绑定、隔离环境下 `--help` 返回 0，并确认仍在运行的 grid 会使本协议明确拒绝启动；没有创建 native 输出，也没有启动第二个 GPU 作业。最初帮助文本检查误用了大写前缀，只修正预检脚本的期望，保留原命令输出。

执行后比较器将核对实际输入、程序与报告，要求 25 个有限 FP32 张量、4,981,760 个元素以及 PNG 与已验证 RAM 基线逐字节相同，并核对进程结束、内存保护、304 次 RAM 权重请求和无缓存。基线 PNG SHA256 为 `bf87214d21461bfda96529830b28a5b7630ef0dcf307c0172f1666fc64044e28`。实际失败必须保留，不能修改协议后复用原计划身份。

可执行冻结目录为 `/var/tmp/ernie-compact-reader512-v1`。这里仅归档协议、源/候选身份、比较器和预检小文件；大模型、程序和张量不进入 Git。compact reader 默认仍关闭，是否启用需要后续独立证据。整个项目的质量、形状、性能与平台验收未完成。
