# 完整生成及组件计时回归

固定64×64、8步、原生文本、Vulkan FP32、CPU direct VAE实际通过。27个trace文件（25个完整有限FP32张量）及PNG与已保存O1 OFF结果逐字节相同。新增计时代码的Net生命周期及失败记录修复已通过代码独审；本次实际执行验证正常生成结果保持一致。

| 组件 | 次数 | 模型加载与准备合计 | 提取与计算合计 |
|---|---:|---:|---:|
| 文本block | 25 | 13.9930 s | 0.8295 s |
| DiT block | 288 | 303.8151 s | 22.5365 s |
| DiT input head | 8 | 6.3237 s | 0.0538 s |
| DiT output head | 8 | 1.5478 s | 0.0152 s |
| VAE decoder | 1 | 0.1049 s | 0.2900 s |

1320条组件记录覆盖330组唯一component/absolute-step/block坐标，每组四个边界，全部complete。CLI总体373.0907秒；顶层已分类371.5132秒，未分类1.5775秒。加载区间包含读取、展开、pipeline创建、上传和内部等待，不能称为纯磁盘时间。组件计时与顶层区间重叠；文本Net销毁在未分类时间中。原始JSON里的“全部嵌套在顶层phase”说明过宽，保留原记录，在源代码里修正说明并通过13项小CLI合同，不为文字改动重跑模型。

实际ncnn分配2375次，同时分配峰968724992字节，1254个allocator世代全部关闭、最终live0。外层393.6474秒，7809个资源样本；scope采样峰9592795136字节，host最低16788799488字节，CPU两核配额与4/6亲和性，swap/max/OOM事件均0。样本memory_current字段存的是累计峰，不是瞬时RSS。

这是trace开启的单次内部回归，旧OFF输出重用；期间其他CPU工作仍在进行。它不构成正式速度或内存比较，也不建立对futz12的优势。即使省去输出head后7次全部加载，当前诊断中理论仅约1.35秒、0.36%；优先改善DiT加载，暂不推进该head缓存。
