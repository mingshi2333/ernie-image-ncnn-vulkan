# 映射加载权重：已实现的实验与实际对照

`ERNIE_EXPERIMENT_MAPPED_MODEL_LOADING` 已接入图像流水线，默认 OFF。使用固定 ncnn 的原生 `use_mapped_model_loading`，Net 持有文件映射直至销毁；模型校验、数学、精度、步数不变。CPU 文本、DiT、VAE 继承此选项，独立 PE 不在范围内；映射失败时 ncnn 可回退普通文件读取。

固定 64×64、原生文本、Vulkan FP32、8 步、CPU direct VAE 实际 exit0。全部 27 个轨迹文件和 PNG 与前次 O2 非映射运行逐字节一致，25 个完整 FP32 张量全部有限。新增二进制 SHA256 为 `1b31d164cebc0f3a81d349bc9778526b28f292a1863e703c39ba045659b80e2e`。

| 测量范围 | 前次普通读取 | 映射加载 |
|---|---:|---:|
| CLI 生成及 PNG 写出 | 373.0907 s | 275.7635 s |
| DiT block 模型加载合计 | 303.8151 s | 206.1195 s |
| DiT block 提取计算合计 | 22.5365 s | 20.5839 s |
| DiT block Net 销毁合计 | 3.0191 s | 7.4509 s |
| 外部运行与核验 | 393.6474 s | 300.5474 s |
| cgroup 采样峰值，含文件缓存 | 9,592,795,136 B | 10,737,418,240 B |
| `memory.events` 的 `max` 计数 | 0 | 466,562 |

该次 CLI 时间少 26.09%，不能据此声称稳定提速或超过 futz12：这是开启轨迹的单次内部对照，复用前次基线，文件缓存未受控，少量 CPU 准备和编译工作同时进行。模型加载区间还包含展开、管线创建、上传与等待，不能把全部差额归为磁盘读取。销毁区间增加，保留该结果。

250ms 外部采样在唯一的原生进程中实际观察到 64 个已绑定模型文件的只读映射，包含 25 个文本 block、36 个 DiT block、两个 head 和 VAE；并非只根据配置推断。全部 1320 个组件边界记录完整。Vulkan 分配次数仍为 2375，同时分配峰值仍为 968,724,992 B；1254 个 allocator 世代全部关闭，最终 live0。

本轮 10 GiB / swap0 / 两核配额（CPU4/6）约束保持不变，最低 host MemAvailable 17,325,006,848 B，OOM/OOM-kill 事件为 0。采样峰值到达 cgroup 限额并有大量 max 事件，表示存在回收压力，不能作为 RSS 降低或内存门槛通过的证据。最终保留默认关闭的实验选项，尚需代表性尺寸、完整质量与配对速度验证。

原始张量和完整源码快照保存在 `outputs/execution-metrics-mapped64-o2-v1`。基线见 [此前完整生成回归](../component-loading-regression/README.md)。
