# Project instructions

## Scope and status

- This is the standalone ERNIE-Image-Turbo ncnn/Vulkan project. Do not modify sibling ncnn or pnnx workspaces as a side effect.
- Start with `README.md`, `sources.lock.json`, `docs/ROADMAP.md`, and the latest artifact report.
- Current implementation plan: `docs/superpowers/plans/2026-09-06-surpass-reference.md`, with six stage plans and `surpass-acceptance.json`. Read `.superpowers/sdd/2026-09-06-surpass-reference/progress.md` for current ownership, measured evidence and remaining work. Targets are not results; preserve historical failures and do not declare superiority until every required gate has evidence.
- Latest runtime-shape implementation: ShapePlan now drives experimental schema-3 dimensions (16..2048, multiples16, area<=2097152), with automatic selection among available independent 32/64/2048 text sources after actual tokenization. Source32 keeps64 DiT slots. All2496 C++ graph instantiations across3 sources/13 geometry points pass canonical checks; this is not execution evidence for every target. Maximum10240 real-weight block: device-weight mode hit the unchanged6144MiB whole-device supervisor guard, no OOM; host-weight mode completed at5609MiB sampled peak, NRMSE2.75455e-6 under unchanged historical tolerance. Larger sequences now use host weights. The new three-source native64 output now has a complete official oracle: all25 tensor gates and PNGmax2 pass, while both inspected images remain gray textures rather than a clear apple. Preserve the initial missing-provenance rejection and do not equate numerical agreement with prompt adherence. Full official/native canary matrix remains open. Read artifacts/2026-09-07/runtime-range-and-buckets/README.md. Preserve schema1/2 limits and all old full1376 failures/results; do not declare F1 or overall superiority complete.
- Latest installed-runtime evidence: artifacts/2026-09-07/runtime-images-and-sdk/README.md. The installed Linux Vulkan binary completes runtime512x512 with the source hidden/network disabled, native15-token text/32 bucket/64 DiT slots, all8 FP32 steps and CPU VAE. All25 tensors/4,981,760 elements pass, PNG MAE0.000361125/max1, actual red apple. Native scope466.868s, cgrouppeak13,910,089,728B/wholeGPU4031MiB/max0/OOM0; these are traced CPU2 diagnostics, not formal speed or RSS. CPU/Vulkan SDK installations and external consumers pass after exporting execution-metrics with private build-only include paths. The official CUDA reference now releases unused cache after each block; first-step8files remain exact, complete reference235.771s/wholeGPU2975MiB/max0/OOM0. Original6155MiB guard termination remains. Both64/512 official fixtures are registered by full digest; complete canary/quality/performance/platform acceptance remains open. All model jobs in this batch finished.
- Latest complete development evidence: `artifacts/2026-09-06/shared-native-pipeline/README.md`. The reviewed 512x384 shared instance ran actual CPU greedy PE, all 25 native text layers with explicit Vector reduction, eight Vulkan FP32 steps and CPU VAE: 25/25 tensor gates, exact PE text/IDs, PNG max1, and all tensors/PNG bitwise equal to the preceding fixed-package native run. This single fixture does not close broad quality, dynamic shapes, performance or platform gates. The original compensated PE/apple 24/25 decoded failure remains historical evidence in `artifacts/2026-09-06/features-and-structure/README.md`.
- Fixed 1024x1024 encoder and production strength-zero reconstruction have independent actual evidence: three encoder and six reconstruction boundaries pass, PNG max1. See `.superpowers/sdd/2026-09-06-surpass-reference/task-F2-strength0-1024-review.md`; fixed strength.5 now also passes all21 compared tensor boundaries and PNGmax1, independently recomputed in task-F2-positive-1024-execution-review.md. Formal15 remains unrun. Linux CPU/Vulkan installed C++ consumption is recorded in `artifacts/2026-09-06/installed-cpp-sdk/README.md`; this is API/installation evidence, not full-model or Windows/macOS acceptance.
- Fixed 1024 strength1 completes all8 steps, but remains quality_gate_failed:24/29 tensor gates pass, predictions3/4/5/7 and decoded fail fixed maximum-error gates; PNGmax2 passes. Full independent review f21ad9f preserves the negative result. Do not infer broad img2img quality from the passing strength0/.5 fixtures. Linux missing-driver diagnostics are fixed and actual CPU5/5/Vulkan6/6 affected CTests pass; new full-model offline execution is tracked separately in the ledger.
- Installed Linux archive now passes the actual fixed 1024 strength.5 offline case with the complete source tree hidden, network disabled and a Chinese-space model path. All nine command outcomes and the exact native PNG match; see `artifacts/2026-09-06/offline-linux-generation/README.md`. Final model verification reaches the 10GiB scope limit with max49634 but no OOM, so do not claim whole-chain max0, a 6.49GB whole-chain peak or S/M acceptance. Other delivery cases and platforms remain open.
- The experimental native generator has passed complete 64x64 and 1024x1024 apple comparisons against staged official modules. After FP32 attention compensation and query chunking, the 40-token English fixture passes 24/25 tensors and PNG (MAE 0.00268, max 1); prediction-7 still fails its maximum-error gate. Chinese compensation passes 21/25 tensors; PNG MAE 0.02263, max 13 still fails. Read artifacts/2026-09-06/attention-parity/README.md and docs/NUMERICAL-DIAGNOSTICS.md for current limits. Historical low-precision failures remain; do not call this broad quality acceptance.
- Use Chinese for progress and technical reports unless the user asks otherwise.
- Fixed 1376x768/s64 now completes actual native text, all8 Vulkan FP32 steps and CPU direct VAE, with a 136-file portable package. Its apple fixture remains quality_gate_failed:23/25 tensors pass; prediction7 and decoded fail maximum-error limits, PNG max3 exceeds fixed max2. Root recomputes all32,098,304 elements. See artifacts/2026-09-07/fixed1376-native-pipeline/README.md. The new package_model.py --fixed-1376x768 accepts only the pinned 1024/s64 source, preserves all weights and uses audited static graph fields; it does not extend schema3. Keep the complete negative result and all earlier component passes.

## Latest runtime verification

Latest additional execution evidence: `artifacts/2026-09-07/runtime-rectangles-and-mapped512/README.md`. The same installed binary/shared source32 package completes native384x512 and512x384 with source hidden/network disabled, native15-token text,8 VulkanFP32 steps and CPU direct VAE. Portrait25/25 passes, PNGMAE0.001034207/max1. Landscape24/25: prediction7 max0.065948 exceeds0.057112, NRMSE0.000660696 passes; PNGMAE0.002382067/max1. Preserve that negative result and both registered official fixtures. The initial portrait official task was supervisor-killed at6767MiB wholeGPU, not OOM; the same-source rerun with additional memory observations completed at3077MiB wholeGPU/1216MiB owned compute. Cause of the first spike remains unproven. Mapped512 completes all28 trace files/25 finite tensors/PNG byte-identical to the previous512 baseline; scope466.868->309.793s is one uncontrolled-cache diagnostic, not formal performance.61 read-only model mappings observed; cgrouppeak16,591,130,624B includes file cache,max/OOM0. Default mapped loading remainsOFF and build-dev is restored byte-exact. All model jobs in this batch finished. Do not repeat these completed cases without a change or unresolved concern; remaining shapes, broad quality, performance and platforms stay open.

- Latest memory-policy work: `artifacts/2026-09-07/adaptive-weight-placement/README.md`. Default Vulkan DiT placement now queries the actual compute heap budget minus process usage before each head/block, combines a configurable 512MiB reserve and the existing large-shape host hint, and requests GPU or RAM weights. Public CLI/API expose auto/device/host and request counts; missing budget is explicit. Controlled512 with an intentionally8192MiB reserve requests RAM for all304 components, completes8 FP32 steps/CPU VAE and is byte-exact across25 tensors/4981760 elements/PNG versus the verified baseline. Scope526.741s, wholeGPU2363MiB, cgrouppeak4618027008B includes file cache, max/OOM0; this is not actual GPU exhaustion, asynchronous OOM recovery, formal performance or allocator-complete residency evidence. CPU/Vulkan affected tests and installed consumers11/11 pass. Activations remain on GPU; bounded cross-step prepared-weight reuse and RAM budget management remain open. Do not rerun this passing case without relevant changes.

- Latest bounded-cache work: `artifacts/2026-09-07/bounded-weight-session/README.md`. Optional Vulkan FP32 `--dit-cache-mib` retains an inspected non-device-local subset in RAM; default0 keeps streaming. Linux availability/cgroup headroom evicts idle entries, unavailable disables admission, and leases preserve active ownership. Actual512 with6GiB cache/3GiB reserve retains6blocks, hits42/loads246, and all25 tensors/4981760 finite elements/PNG match the previous no-cache baseline byte-for-byte. Scope469.113s versus previous526.741s is a single uncontrolled-cache diagnostic, not formal speed. Charged cachepeak5687678976B; wholeGPU2376MiB/cgrouppeak12277387264B includes file cache/max0/OOM0. Fourteen affected CPU/Vulkan/installed-consumer checks pass. No prefetch, activation spilling or failed-command recovery; cross-platform RAM readers, broader development fixtures and O2/S/M acceptance remain open. All model jobs finished; do not rerun the passing case without a change or a defined paired experiment.

## Latest benchmark integration

- `artifacts/2026-09-07/benchmark-runtime/README.md` records schema-3/runtime dimensions and loading/placement/cache/thread/device controls in `tools/benchmark_pipeline.py`, with lightweight native `--report-json` and verified post-tokenization source selection. All-file package verification and frozen identity checks remain; trace/allocation instrumentation disable speed eligibility; logs never certify identity. Initial 12 CPU/Vulkan/API/CLI/installed-consumer CTests and 36 Python checks pass. Frozen669bindings actual shared512, 15 native tokens/source32/64DiTslots,8VulkanFP32steps/CPUdirectVAE,2threads,mapped,6GiBcache/3GiBreserve/deliberate8192MiBGPUreserve runs with traceOFF, producing byte-exact PNG. 262RAM requests,42hits/246loads/6cached blocks/0pressure evictions; wrappernative386.854s versus supervisor404.478s; nativeRSS9560916KiB,wholeGPU2336MiB,cgrouppeak17180102656B/max347682/OOM0. These scopes differ from old traced diagnostics and do not prove speedup. Raw full-run binary20ab4f77... has a documented text-timing label error; after frozen comparison, corrected only report description/legacy text_seconds key plus test path convention. Final binaryff2f934f... was rebuilt and affected tests passed; full model was not rerun for metadata-only corrections. Exact4-file before/after evidence is retained; inference source stayed unchanged. Model/Python snapshots and original project isolation are explicit; native executable still needs no Python. All jobs finished; ncnn clean/pinned. Next: predeclared same-final-binary repeated mapped/stdio and cacheOFF/ON comparisons. O1/O2/S/M/fullquality/shapes/platforms and full surpass goal remain open; no push/release.

## Code organization

- Latest loading-policy evidence: `artifacts/2026-09-07/runtime-model-loading/README.md`. Runtime `--model-loading default|stdio|mapped` permits one binary to choose image text/DiT/VAE loading; default preserves the existing CMake setting, OFF. PE is unchanged. Sixteen effective CPU/Vulkan/API/CLI/installed-consumer checks pass, including both FP32/BF16 files and mapped cache lifetimes. Full512 mapped+6GiB cache preserves all25 tensors/4981760 finite values and PNG exactly, but reaches the16GiB cgroup with max45089, OOM0,12evictions and0hits. Original minimum-hit checker fails and remains preserved; additional review has all_checks_pass=false. WholeGPU2372MiB, cgrouppeak17179869184B includes file cache and is not RSS, scope386.096s is traced/unpaired. Both mapping and cache remain opt-in; do not claim combined reuse benefit or close O2/S/M. All jobs finished.

- Latest host-headroom evidence: `artifacts/2026-09-07/cache-file-lru/README.md`. Dedicated host_memory module now credits at most half of clean active+inactive file LRUs, bounded by non-shmem file, usage, MemAvailable and every finite cgroup ancestor. Linux source df290809 is authenticated; strict parsing, migration and true pressure remain covered. Same mapped512 inputs/budgets/limits and396 bindings yield42hits/246loads/6admissions/0pressure evictions, all25 tensors/4981760 finite values/PNG byte-exact; CPU/Vulkan/installed-consumer checks10/10 pass. Cache retention improves over preceding13hits/54evictions, but scope384.364s versus378.970s is traced/unpaired and provides no speedup evidence. Cgroupmax events rise to354168, OOM0; observed cgrouppeak17180102656B briefly exceeds the unchanged16GiB configured limit by233472B. WholeGPU2349MiB, observed native VmHWM9801760768B; file-cache-inclusive cgroup usage is not RSS and category peaks must not be summed. Cache remainsOFF. Both prior13hit and zero-hit records remain. All jobs finished; next performance-tool work needs shared-package/runtime-shape/memory-option support before same-binary repeated comparisons. O2/S/M/shapes/quality/platforms and general activation/OOM recovery remain open.


- Read docs/CODE-STRUCTURE.md. Public generation types live in include/ernie/pipeline.h; cli/ owns arguments and PNG I/O; src/pipeline.cpp owns stage orchestration. Keep model math and cache sessions in their components.
- Keep the public header independent of ncnn/PNG headers. The pipeline returns RGB pixels and reports progress through callbacks.
- Root CMake delegates dependencies, tokenizer, runtime, CLI, probes and tests to their own directories. Keep documented executable paths in the build root.
- tools/source_inventory.py must cover all source and build directories in new evidence. Preserve old snapshots and command compatibility.

## Evidence and correctness

- Numerical tolerances and concrete S/M/blind-review targets were selected by the assistant for this project, not supplied as ERNIE/ncnn/futz12 standards or exact user requirements. The original pixel gates were small-fixture diagnostics and remain uncalibrated for broad perceptual quality or backend variation. Read docs/NUMERICAL-DIAGNOSTICS.md for provenance. Preserve old values/results. Do not infer execution failure, visible quality loss or memory exhaustion from a numerical threshold alone. Any recalibration needs a new reasoned protocol version and must not use the frozen formal cases for tuning.
- Pin upstream revisions. A feature in a local modified ncnn checkout is not evidence that upstream has it.
- Preserve official ERNIE mathematics: tokenizer behavior, hidden-state selection, YaRN, DiT RoPE, shared AdaLN, GELU gating, latent packing, BN normalization, and sigma schedule.
- Keep CPU and Vulkan cache handles opaque. CPU extraction uses `type=1`. Use a distinct session cache allocator, consume-and-replace handles, and release caches before allocator destruction. Do not use shallow copies as independent session snapshots.
- Optional PE is actual Ministral3: 26 layers, final norm and tied LM head, with a distinct PE tokenizer/chat template. Greedy CPU FP32 315-token EOS and every logit passed the fixed 2e-4 gates. Do not confuse this with the 25-layer image text encoder.
- PE input/output limits are 2048 each; native cache capacity is at most 4096, below the official 16384-position query-scaling boundary. Prefill is sequential, without padded cache entries. Same sampled seed across C++ and PyTorch is not a parity oracle.
- The 512x384, 1080-token image fixture runs to completion in FP32 and BF16 but fails the fixed full-quality gates (19/25 and 11/25 tensors; PNG max 17 and 255). Keep BF16 experimental.
- Do not reuse DiT K/V across denoising steps as an exact optimization. Approximate caching needs a separate opt-in experiment and quality gate.
- Use identical saved latents and embeddings for full-model parity. An identical integer seed across frameworks is insufficient.
- State whether a result is source inspection, synthetic operator validation, real-weight validation, or an end-to-end benchmark.
- Do not claim speedup from buffer counts or from changing model variants, step counts, precision, or PE settings.
- Use `tools/prepare_block.py` to retain the reviewed ERNIE GELU/RoPE semantics. The pinned upstream Vulkan GELU uses a tanh approximation; standard RotaryEmbed assumes a shared half-width angle table.
- Register the project's RMSNorm and final LayerNorm wrappers. Native FP16 intermediate squares can overflow; the wrappers promote normalization on device. Final LayerNorm is non-affine, width 4096, and conditioning has no SiLU.
- Real text causes DiT residual activations to exceed 65504. FP16 inference requires both residual sums to use ErnieResidualAdd and retain the FP32 skip path. Normalized projections return to model storage precision. Never silently clamp the residual.
- The FP32 Euler master latent is checked after each step. Vulkan downloads only 128 status floats for this check; tracing downloads activations separately and changes measurement scope.
- The pinned Mistral3 text configuration dispatches MistralModel, not Ministral3. Required hidden_states[-2] is block 24 output: 25 blocks, without final norm. Independently exported text buckets are 32, 64 and 2048 tokens, including BOS. The 1080-token official-weight text fixture passes at NRMSE 7.70e-6.
- CPU VAE uses FP64 mean/centered-variance reductions with FP32 activations and affine arithmetic. Large native GroupNorm reductions failed the fixed numerical gate before this correction.
- CPU VAE defaults to direct convolution with Winograd disabled. The 1024 standalone fixture passes at NRMSE 9.41e-7 and 5.42 GiB peak RSS; the complete apple run uses 5.82 GiB. The old SGEMM mode remains explicit for comparisons.
- Single-step teacher forcing is a diagnostic, not a substitute for free-running parity. Step 6 of the long FP32 fixture passes with official input tensors; the full trajectory still fails. Do not loosen existing gates or discard either result.
- BF16 standalone gates fail for blocks 31, 33, and 35 on the current synthetic fixture. Do not erase those failures or relax gates after observing results. Connected predictions are a different fixture scope.
- Treat BF16 ModelBin packing as lossless on-disk storage only when the full reconstructed FP32 stream hash matches. Loading still expands data and does not establish a peak memory reduction.
- Export a separate static graph for each token bucket. A matching operator list does not prove shape compatibility.
- VAE spatial specialization is limited to the full-hash-reviewed flatten/unflatten pair, and requires an independently executed target-resolution official fixture. Preserve the failed high-resolution whole-graph pnnx conversion; do not re-run its unbounded memory expansion.
- FP32 non-Flash attention uses compensated softmax/P@V reductions and at most 128 query rows per chunk, retaining all K/V. Check both pinned shader SHA256 values; never bypass a hash failure when updating ncnn. Chunk completions count as compute submissions and do not download activations. Preserve native cache and low-storage Flash paths.
- Reference-embedding runs bypass the native text encoder and are diagnostics even if they pass. The CPU FP64 RMSNorm candidate was rejected: its first-layer gain did not improve the complete text encoder. Do not promote local operator improvements to full-model acceptance.
- Record failures and skipped tests. Avoid repeating passing checks without a change or unresolved concern.

## Resource use and delivery

- The initial target is an 8GB GPU and 32GB system RAM. Inspect current availability before large runs. Begin with one block and staged weight loading.
- Keep weights, build products and generated images out of Git. Store small manifests, measurements and source hashes in `artifacts/`.
- Schema-2 portable packages contain all 136 runtime files with no internal symlinks. Both native and Python checks cover every runtime checksum, file size, revision and model.cfg agreement. Keep schema-1 development package compatibility.
- Snapshot runner binaries before long validation jobs. A concurrent build can temporarily remove or replace its executable; the first batch recorded this failure.
- Run large GPU jobs sequentially. Overlapping a VAE test and DiT generation caused allocation failures and a SIGSEGV; that run does not establish an isolated-device capacity limit.
- The isolated full-matrix FP32 compensation long run also hit allocation failure and SIGSEGV during step 3. Preserve it separately from the earlier overlapping-job failure. Query chunking subsequently completed all eight steps with a 4098 MiB whole-device sampled peak; this is not an allocator peak guarantee.
- External publication, push and release require user authorization. Local construction and tests are within the approved project scope.

## Active repeated memory experiment

- The new four-configuration within-project development grid is frozen and started: artifacts/2026-09-07/memory-grid-protocol/README.md. Same finalff2f934f binary from cleanb6b80d3, shared512, explicit host weights, stdio/mapped × cache0/6144MiB,1warmup+3measured each,16runs total.374common frozen file bindings plus16subplans; source snapshots are readonly and original project hidden. Six synthetic orchestration checks and isolated preflight pass. This artifact contains preparation, not complete performance results. Inspect /var/tmp/ernie-memory-grid512-v1/progress.json and actual master/supervisor/native processes before continuing or restarting; unified session63899 was live at launch. Preserve any failure, never retry into the same output. No aggregate until all sixteen pass. Existing resource controls/defaults and formal S/M/fullquality/shapes/platform/fullgoal remain unchanged; no push/release.

- Completed update: `artifacts/2026-09-07/memory-grid512/README.md` records all16 runs successful, byte-exact PNGs and zero OOM/OOM-kill, original master exited and frozen analyzer recomputed the same f26721c4... summary. Three measured wall medians: stdio-off500.252s, stdio-cache465.762s, mapped-off395.931s, mapped-cache393.330s; corresponding native peak RSS medians1.824/6.276/1.947/9.102GiB. Paired elapsed reduction mapped-off vs stdio-off20.85%, mapped cache vs off0.85%; no formal peer/general-performance claim. Mapped modes hit the16GiB file-cache-inclusive cgroup limit, maximum observed exceedance188416B, no OOM. Page cache/background load uncontrolled; cache and mapping remain opt-in. No model job from this grid remains; never restart it. A separately frozen8158dc1 compact-reader regression has now started in /var/tmp/ernie-compact-reader512-v1, session72431, after this grid ended. Inspect actual process.json/command log before resuming. It is traced and not part of the grid or a Windows-patch regression.

## Opt-in compact reader candidate

- `artifacts/2026-09-07/compact-model-reader/README.md` records a two-line correction to the pinned ncnn FP16/BF16 ordinary-read temporary vector element count. `ERNIE_EXPERIMENT_COMPACT_MODEL_READER` defaults OFF, authenticates the exact original, and compiles a build-directory copy without modifying ncnn. Actual ModelBin tests observe an 8,388,612B payload using16,777,224B temporary storage OFF and8,388,612B ON, with bit-exact decoding, alignment/reference-path/truncation checks. Three affected CPU-executed CTests pass in CPU/Vulkan builds each with OFF/ON, twelve executions and zero failures/skips; this includes no GPU model run. Added Linux CPU-ON CI configuration is locally parsed, not remotely executed. Candidate Vulkan binaryb15ccce8... is saved in outputs/compact-model-reader-v1/candidate/vulkan/ernie-image. Both normal builds restoredOFF; ncnn archives and Vulkan CLIff2f934f... match originals exactly, pinned checkout clean. Complete-model/speed validation of the candidate must wait until the active frozen grid finishes; never overlap model jobs or attribute the grid's old-reader results to this patch. No default promotion, push/release or full-goal completion.

- Compact-reader execution is now complete: `artifacts/2026-09-07/compact-reader-regression/README.md`. Frozen candidate b15ccce8/plan45d43302 passes all25 finite tensors/4,981,760 values and PNG byte-exact against the old512 baseline, all8 steps,304RAM requests/cache0. Scope443.709s includes preparation/trace; cgrouppeak2817376256B includes file cache, sampled nativeVmHWM2085281792B, wholeGPU2308MiB, max/OOM0. Native PID3102831 and cgroup ended, unified72431 returned0. This traced single-case result is not paired performance, true exhaustion/recovery, or a Windows-patch model test. Do not rerun unchanged. Candidate/default and all wider acceptance gaps stay unchanged. No model jobs from this grid/candidate batch remain.

## Windows portability development evidence

- `artifacts/2026-09-07/windows-cpu-portability/README.md` records actual MinGW x64/Rust Windows GNU cross-compilation and CPU execution under Wine. Fixed Rust target/output naming/system links, Windows working-set/PID portability, exclusive report creation, and explicit UTF-16 CLI/UTF-8 API/native file boundaries. Final Windows C++ 28/28, CLI/report 35/35 and Linux CPU CTest 35/35 pass. Relocated SDK v4 hides original source/build and network, passes all 13 expected command outcomes including Unicode/emoji paths and the external C++ consumer. This is not native Windows/MSVC/GPU/full-model or macOS acceptance.
- Preserve initial compile failure, report overwrite, three POSIX/Wine symlink failures, SDK v1 temporary-directory failure, SDK v2 Unicode failure and SDK v3 Wine cleanup timeout. Three additional scenarios created through actual Windows link APIs are rejected as expected; they do not overwrite the old failures. Final Windows CLI SHA256 is 5c1706e89d8137d408e2b2fc459e6eb34ba778b5eb27bc578e4f06e6a12e4209, with 334 source bindings and a patch reconstructing all final sources from 8158dc1. Local build-dev has subsequently been rebuilt with these path changes; the active memory grid and prepared compact-reader regression still use their separately frozen older binary/source. No repeated full-model run for path-only corrections. Full quality/shapes/platforms/O1/O2/S/M and surpass goal remain open; no push/release.

## Latest Windows memory reader

- `artifacts/2026-09-07/windows-memory-headroom/README.md` records Windows physical/commit/address-space headroom queries, while Wine and Windows Job processes retain streaming without optional cache admission. The initial Wine API estimate ignored a configured4GiB Linux scope; preserve that negative result and the initial optional wineserver-helper exit2. Final actualWine rejection and WindowsJob assignment checks pass with Linux/Windows2CPUtests each, CLIhelp and owned-prefix cleanup. NativeWindows noJob cache/Vulkan/fullmodel, Job ancestor budgets andmacOSreader remain unverified/open; this is not allocation/OOM recovery evidence. Defaultcache0 and Linuxpolicy unchanged. Builds now have WindowsCLI46df787c andLinuxCPUCLI30cae0f3; frozenmodelcanaries continue using92559ea4/990e8ef independently. No push/release.
